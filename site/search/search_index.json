{"config":{"lang":["en"],"separator":"[\\s\\-]+","pipeline":["stopWordFilter"]},"docs":[{"location":"","title":"Introduction to Ubuntu Server","text":"<p>Ubuntu Server fundamentals</p> <p>This site presents Canonical Ubuntu Basic training material.</p> <p>This material is copyright of Canonical Limited. This material may be used for personal and non-commercial use only.</p> <p>This documentation is copyright of Canonical Limited. You are welcome to display on your computer, download and print this documentation or to use the hard copy provided to you for personal, education and non-commercial use only. You must retain copyright, trademark and other notices unaltered on any copies or printouts you make. Any trademarks, logos an service marks displayed in this document are property of their owners, whether Canonical or third parties.</p> <p>This documentation is provided on an \"as is\" basis, without warranty of any kind, either express or implied. Your use of this documentation is at your own risk. Canonical disclaims all warranties and liability that may result directly or indirectly from the use of this documentation.</p> <p>Note</p> <p>Command validation is pending.</p>"},{"location":"backup-and-recovery/","title":"9. Backup and Recovery","text":"<p>Note</p> <p>This page is structured from the source material. Commands are pending validation.</p> <p>Abstract</p> <p>Use compression, tar archives, rsync, and backup recovery procedures.</p>"},{"location":"backup-and-recovery/#91-using-archiving-and-compression-utilities","title":"9.1 Using Archiving and Compression Utilities","text":"<p>Compression reduces file size. Backups and archives are commonly stored in compressed form. On Linux, an archive commonly means a tar file that groups files and directories for distribution, storage, backup, or manipulation.</p>"},{"location":"backup-and-recovery/#gzip-compression","title":"gzip compression","text":"<p>gzip is fast, memory-efficient, and widely compatible.</p> <pre><code># Compress a file with gzip.\ngzip myfile\n</code></pre> Expected result <p>Validation pending; no captured output is available.</p> <pre><code># Decompress a gzip file.\ngzip -d myfile.gz\n</code></pre> Expected result <p>Validation pending; no captured output is available.</p> <pre><code># Display gzip file statistics.\ngzip -l myfile.gz\n</code></pre> Expected result <p>Validation pending; no captured output is available.</p>"},{"location":"backup-and-recovery/#bzip2-compression","title":"bzip2 compression","text":"<p>bzip2 provides better compression than gzip but usually takes longer and uses more memory while compressing.</p> <pre><code># Compress a file with bzip2.\nbzip2 myfile\n</code></pre> Expected result <p>Validation pending; no captured output is available.</p> <pre><code># Decompress a bzip2 file.\nbzip2 -d myfile.bz2\n</code></pre> Expected result <p>Validation pending; no captured output is available.</p> <pre><code># Compress a file at compression level 1.\nbzip2 -1 myfile\n</code></pre> Expected result <p>Validation pending; no captured output is available.</p>"},{"location":"backup-and-recovery/#xz-compression","title":"xz compression","text":"<p>xz uses LZMA2. It provides a higher compression ratio but can take significantly longer and use more memory than gzip or bzip2; decompression is faster than bzip2.</p> <pre><code># Compress a file with xz.\nxz myfile\n</code></pre> Expected result <p>Validation pending; no captured output is available.</p> <pre><code># Decompress an xz file.\nxz -d myfile.xz\n</code></pre> Expected result <p>Validation pending; no captured output is available.</p> <pre><code># Compress with xz at maximum extreme compression.\nxz -e -9 myfile\n</code></pre> Expected result <p>Validation pending; no captured output is available.</p> <pre><code># Display xz file statistics.\nxz -l myfile.xz\n</code></pre> Expected result <p>Validation pending; no captured output is available.</p>"},{"location":"backup-and-recovery/#92-tar-archiving","title":"9.2 Tar Archiving","text":"<p><code>tar</code> means tape archiving. A tar archive gathers files into one file without compression; a tarball is a common name for such an archive.</p> <pre><code># Create an archive from two files.\ntar cvf myfiles.tar myfile1 myfile2\n</code></pre> Expected result <p>Validation pending; no captured output is available.</p> <p><code>c</code> creates an archive, <code>v</code> lists processed files verbosely, and <code>f</code> precedes the archive filename.</p> <pre><code># Extract an archive.\ntar xvf myfiles.tar\n</code></pre> Expected result <p>Validation pending; no captured output is available.</p> <pre><code># List archive contents.\ntar tvf myfiles.tar\n</code></pre> Expected result <p>Validation pending; no captured output is available.</p> <pre><code># Extract an archive into another directory.\ntar xvf /mnt/mydir.tar -C /home/alex/temp\n</code></pre> Expected result <p>Validation pending; no captured output is available.</p>"},{"location":"backup-and-recovery/#tar-with-gzip","title":"Tar with gzip","text":"<p>Use <code>-z</code> with tar for gzip compression.</p> <pre><code># Create a gzip-compressed tar archive.\ntar czvf myfile.tar.gz myfile\n</code></pre> Expected result <p>Validation pending; no captured output is available.</p> <pre><code># List a gzip-compressed tar archive.\ntar tzvf myfile.tar.gz\n</code></pre> Expected result <p>Validation pending; no captured output is available.</p> <pre><code># Extract a gzip-compressed tar archive.\ntar xzvf myfile.tar.gz\n</code></pre> Expected result <p>Validation pending; no captured output is available.</p>"},{"location":"backup-and-recovery/#tar-with-bzip2","title":"Tar with bzip2","text":"<p>Use <code>-j</code> with tar for bzip2 compression.</p> <pre><code># Create a bzip2-compressed tar archive.\ntar cjvf myfile.tar.bz2 myfile\n</code></pre> Expected result <p>Validation pending; no captured output is available.</p> <pre><code># List a bzip2-compressed tar archive.\ntar tjvf myfile.tar.bz2\n</code></pre> Expected result <p>Validation pending; no captured output is available.</p> <pre><code># Extract a bzip2-compressed tar archive.\ntar xjvf myfile.tar.bz2\n</code></pre> Expected result <p>Validation pending; no captured output is available.</p>"},{"location":"backup-and-recovery/#tar-with-xz","title":"Tar with xz","text":"<p>Use <code>-J</code> with tar for xz compression.</p> <pre><code># Create an xz-compressed tar archive.\ntar cJvf myfile.tar.xz myfile\n</code></pre> Expected result <p>Validation pending; no captured output is available.</p> <pre><code># List an xz-compressed tar archive.\ntar tJvf myfile.tar.xz\n</code></pre> Expected result <p>Validation pending; no captured output is available.</p> <pre><code># Extract an xz-compressed tar archive.\ntar xJvf myfile.tar.xz\n</code></pre> Expected result <p>Validation pending; no captured output is available.</p>"},{"location":"backup-and-recovery/#93-using-rsync","title":"9.3 Using rsync","text":"<p>rsync copies and synchronizes files and directories locally or remotely, across directories, disks, and networks. It supports copying links, devices, owners, groups, and permissions, and can compress data during transfer.</p> <pre><code># Use the general rsync syntax.\nrsync options SOURCE DESTINATION\n</code></pre> Expected result <p>Validation pending; no captured output is available.</p> <p>Common options are <code>-v</code> for verbose output, <code>-r</code> for recursive copying, <code>-a</code> for archive mode, <code>-z</code> to compress data, and <code>-P</code> to show progress.</p> <pre><code># Synchronize a local file to a backup directory.\nrsync -avP file.tar /path-to/backups/\n</code></pre> Expected result <p>Validation pending; no captured output is available.</p> <pre><code># Synchronize a local directory to a backup directory.\nrsync -avP /home/user/ /path-to/backups/\n</code></pre> Expected result <p>Validation pending; no captured output is available.</p> <pre><code># Synchronize a local directory and delete destination-only files.\nrsync -avP --delete /home/user/ /path-to/backups/\n</code></pre> Expected result <p>Validation pending; no captured output is available.</p> <p>Warning</p> <p><code>--delete</code> removes destination files that are absent from the source. Confirm the source and destination before running it.</p> <pre><code># Synchronize a local directory to a remote server.\nrsync -azvP /home/user/ user@10.10.10.10:/pathto/backup/\n</code></pre> Expected result <p>Validation pending; no captured output is available.</p> <pre><code># Synchronize a remote directory to a local directory.\nrsync -azvP user@10.10.10.10:/path-to/backup/ /tmp/local/\n</code></pre> Expected result <p>Validation pending; no captured output is available.</p> <p>Use <code>-z</code> to compress data during network transfer. <code>--compress-level=NUM</code> sets an explicit compression level.</p>"},{"location":"backup-and-recovery/#using-rsync-with-ssh","title":"Using rsync with SSH","text":"<p>Use <code>-e ssh</code> to transfer data through SSH.</p> <pre><code># Copy a remote file locally through SSH.\nrsync -azvPe ssh user@10.10.10.10:/path-to/file.txt /tmp/\n</code></pre> Expected result <p>Validation pending; no captured output is available.</p> <pre><code># Copy a local file to a remote location through SSH.\nrsync -azvPe ssh file.txt user@10.10.10.10:/pathto/backups/\n</code></pre> Expected result <p>Validation pending; no captured output is available.</p>"},{"location":"backup-and-recovery/#include-exclude-and-transfer-limits","title":"Include, exclude, and transfer limits","text":"<pre><code># Include names beginning with A and exclude all other names.\nrsync -azvPe ssh --include 'A*' --exclude '*' user@10.10.10.10:/path-to/files /tmp\n</code></pre> Expected result <p>Validation pending; no captured output is available.</p> <pre><code># Transfer files no larger than 500k from a remote log directory.\nrsync -azvPe ssh --max-size='500k' 10.10.10.10:/var/log/ /tmp/\n</code></pre> Expected result <p>Validation pending; no captured output is available.</p> <pre><code># Remove source files after successful local transfer.\nrsync -azvP --remove-source-files file.txt /tmp/\n</code></pre> Expected result <p>Validation pending; no captured output is available.</p> <p>Warning</p> <p><code>--remove-source-files</code> deletes source files after transfer. Confirm the destination before running it.</p> <pre><code># Preview an rsync transfer without changing files.\nrsync -azvP --dry-run file.txt /tmp/\n</code></pre> Expected result <p>Validation pending; no captured output is available.</p> <pre><code># Limit an SSH rsync transfer to 100 KBytes per second.\nrsync --bwlimit=100 -azvPe ssh /path-to/files/ user@10.10.10.10:/path-to/backup/\n</code></pre> Expected result <p>Validation pending; no captured output is available.</p>"},{"location":"backup-and-recovery/#94-backup-and-recovery-lab","title":"9.4 Backup and Recovery Lab","text":"<p>This lab uses tar to create, list, update, delete, extract, and gzip-compress archives.</p> <pre><code># Open the tar manual.\nman tar\n</code></pre> Expected result <p>Validation pending; no captured output is available.</p> <pre><code># Create a tar archive of /etc.\nsudo tar cf /tmp/myetc.tar /etc\n</code></pre> Expected result <p>Validation pending; no captured output is available.</p> <pre><code># List the archive contents.\ntar tf /tmp/myetc.tar\n</code></pre> Expected result <p>Validation pending; no captured output is available.</p> <pre><code># Add root's bashrc to the archive.\nsudo tar uvf /tmp/myetc.tar /root/.bashrc\n</code></pre> Expected result <p>Validation pending; no captured output is available.</p> <pre><code># Verify root's bashrc is in the archive.\ntar tf /tmp/myetc.tar | grep root/.bashrc\n</code></pre> Expected result <p>Validation pending; no captured output is available.</p> <p>Danger</p> <p>The next command deletes an entry from the archive. Confirm the archive and entry name before running it.</p> <pre><code># Delete root's bashrc from the archive.\nsudo tar --delete -f /tmp/myetc.tar root/.bashrc\n</code></pre> Expected result <p>Validation pending; no captured output is available.</p> <pre><code># Verify root's bashrc is no longer in the archive.\ntar tf /tmp/myetc.tar | grep root/.bashrc\n</code></pre> Expected result <p>Validation pending; no captured output is available.</p> <pre><code># Change to the temporary directory.\ncd /tmp\n</code></pre> Expected result <p>Validation pending; no captured output is available.</p> <pre><code># Extract etc/hostname from the archive.\ntar xvf myetc.tar etc/hostname\n</code></pre> Expected result <p>Validation pending; no captured output is available.</p> <pre><code># Create an extraction directory and enter it.\nmkdir /tmp/tar &amp;&amp; cd /tmp/tar\n</code></pre> Expected result <p>Validation pending; no captured output is available.</p> <pre><code># Extract all archive contents.\ntar xvf /tmp/myetc.tar\n</code></pre> Expected result <p>Validation pending; no captured output is available.</p> <pre><code># Return to the temporary directory.\ncd /tmp\n</code></pre> Expected result <p>Validation pending; no captured output is available.</p> <pre><code># Compress the tar archive with gzip.\nsudo gzip myetc.tar\n</code></pre> Expected result <p>Validation pending; no captured output is available.</p> <pre><code># Create a gzip-compressed archive of /etc.\nsudo tar czvf /tmp/myetc2.tgz /etc\n</code></pre> Expected result <p>Validation pending; no captured output is available.</p> <pre><code># Create a gzip extraction directory and enter it.\nmkdir /tmp/tgz &amp;&amp; cd /tmp/tgz\n</code></pre> Expected result <p>Validation pending; no captured output is available.</p> <pre><code># Extract the gzip-compressed archive.\ntar xzvf /tmp/myetc2.tgz\n</code></pre> Expected result <p>Validation pending; no captured output is available.</p> <pre><code># Return to the temporary directory.\ncd /tmp\n</code></pre> Expected result <p>Validation pending; no captured output is available.</p> <p>Danger</p> <p>The next command recursively removes the listed files and directories from <code>/tmp</code>.</p> <pre><code># Remove the temporary files and directories created by this lab.\nsudo rm -rf tar tgz etc myetc.tar.gz myetc2.tgz\n</code></pre> Expected result <p>Validation pending; no captured output is available.</p> <pre><code># Return to the home directory.\ncd ~\n</code></pre> Expected result <p>Validation pending; no captured output is available.</p> <p>End of the lab. Do not continue to the next topic.</p>"},{"location":"cli-environment/","title":"2. CLI Environment","text":"<p>Note</p> <p>This page is structured from the source material and is pending command validation.</p> <p>Abstract</p> <p>Define the shell, use CLI commands and help, work with variables and streams, chain commands, and edit files with nano.</p> <p>The shell is a command-language interpreter for interacting with the operating system. It receives typed commands and sends them to the operating system for execution. Ubuntu Server installs <code>bash</code> and <code>dash</code> by default; <code>bash</code> is the default shell. System scripts declare the shell they use.</p> <ul> <li><code>bash</code>: Bourne Again Shell</li> <li><code>dash</code>: Debian Almquist Shell</li> </ul> <p>The user's default shell is defined in <code>/etc/passwd</code>. The distribution-agnostic command-line interface (CLI) provides more control and options than a graphical user interface. A terminal emulator provides access to the shell and normally displays a prompt containing the username, host name, and a dollar sign.</p> <p></p> <p>Commands can be executable programs, shell built-ins such as <code>cd</code>, shell functions, or aliases. Terminal text can be copied with the mouse by highlighting it, then using the middle mouse button or the paste option in the terminal.</p>"},{"location":"cli-environment/#21-secure-shell","title":"2.1 Secure Shell","text":"<p>Secure Shell (SSH) connects to remote computers and allows interactive program execution. SSH is the common method for secure remote Linux-server access: it encrypts the session and supports authentication, allowing one PC to manage multiple computers.</p> <p>Use <code>ssh remote_host</code> when the remote username matches the local user, or <code>ssh user@remote_host</code> when it differs. Use <code>exit</code> to end an SSH session. To run a command remotely, use <code>ssh remote_host command_to_run</code>. These are generic connection forms, not executable lab commands.</p>"},{"location":"cli-environment/#22-cli-commands","title":"2.2 CLI Commands","text":"<p>Common CLI commands can also be used in scripts to automate tasks.</p> <p>Navigating the filesystem: <code>cd</code> changes directory, <code>ls</code> lists files, <code>mkdir</code> creates a directory, <code>pwd</code> prints the working directory, and <code>rmdir</code> removes a directory.</p> <p>Manipulating the filesystem: <code>chgrp</code> changes group ownership, <code>chmod</code> changes file mode bits, <code>chown</code> changes file owner and group, <code>df</code> reports filesystem space use, and <code>du</code> estimates file space use.</p> <p>Searching the filesystem: <code>find</code> searches for files matching a pattern, <code>locate</code> finds files by name, and <code>which</code> locates a command.</p> <p>Searching file contents: <code>file</code> determines file type, <code>grep</code> prints matching lines, and <code>less</code> and <code>more</code> view content.</p> <p>Manipulating file contents: <code>cp</code> copies, <code>ln</code> creates a symbolic link, <code>mv</code> moves or renames, <code>rm</code> removes, and <code>touch</code> changes timestamps.</p> <p>Text processing: <code>cat</code> concatenates files, <code>gawk</code> scans and processes patterns, <code>head</code> displays the first part of output, <code>tail</code> displays the last part, and <code>tee</code> writes input to standard output and a file.</p>"},{"location":"cli-environment/#221-cli-commands-lab","title":"2.2.1 CLI Commands Lab","text":"<ol> <li>Use <code>man</code> to identify four <code>ls</code> options.</li> </ol> <pre><code># Open the ls manual page\nman ls\n</code></pre> Expected result <p><code>man</code> opened the <code>ls</code> page and exited normally after <code>q</code>.</p> <pre><code>man: can't set the locale; make sure $LC_* and $LANG are correct\n</code></pre> <p><code>-a, --all</code> includes entries beginning with <code>.</code>, <code>-h, --human-readable</code> prints readable sizes with <code>-l</code> or <code>-s</code>, <code>-l</code> uses long format, and <code>-t</code> sorts by newest modification time first.</p> <ol> <li>Create <code>mydir1</code>, enter it, create and enter <code>mydir2</code>, return to its parent, then remove <code>mydir2</code>.</li> </ol> <pre><code># Create mydir1\nmkdir mydir1\n</code></pre> Expected result <p>No output.</p> <pre><code># Enter mydir1\ncd mydir1\n</code></pre> Expected result <p>No output.</p> <pre><code># Create mydir2\nmkdir mydir2\n</code></pre> Expected result <p>No output.</p> <pre><code># Enter mydir2\ncd mydir2\n</code></pre> Expected result <p>No output.</p> <pre><code># Return to the parent of mydir2\ncd ..\n</code></pre> Expected result <p>No output.</p> <pre><code># Remove the empty mydir2 directory\nrmdir mydir2\n</code></pre> Expected result <p>No output.</p> <ol> <li>Create <code>file1</code>, copy it to <code>file2</code>, return to the parent of <code>mydir1</code>, create <code>mynewdir</code>, and copy <code>mydir1</code> contents into it.</li> </ol> <pre><code># Create file1\ntouch file1\n</code></pre> Expected result <p>No output.</p> <pre><code># Copy file1 to file2\ncp file1 file2\n</code></pre> Expected result <p>No output.</p> <pre><code># Return to the parent directory\ncd ..\n</code></pre> Expected result <p>No output.</p> <pre><code># Create mynewdir\nmkdir mynewdir\n</code></pre> Expected result <p>No output.</p> <pre><code># Copy mydir1 contents into mynewdir\ncp -a mydir1/* mynewdir\n</code></pre> Expected result <p>No output.</p> <ol> <li>Type <code>ls my</code> and press Tab twice to inspect shell completion choices.</li> </ol> <pre><code># Begin a listing command for tab completion\nls my*\n</code></pre> Expected result <pre><code>mydir1:\nfile1  file2\n\nmynewdir:\nfile1  file2\n</code></pre> <p>End of the lab. Do not continue to the next topic.</p>"},{"location":"cli-environment/#23-getting-help","title":"2.3 Getting Help","text":"<p><code>man</code> is the system manual pager. Its argument identifies a program, utility, or function page. A page can occur in multiple sections; by default, the first matching section in a predefined order is displayed. <code>man man</code> documents sections, display options, exit status, environment variables, and the <code>SEE ALSO</code> section.</p> <p>Manual-page sections are: 1 executable programs or shell commands; 2 system calls; 3 library calls; 4 special files, usually in <code>/dev</code>; 5 file formats and conventions; 6 games; 7 miscellaneous material; 8 system-administration commands; and 9 non-standard kernel routines.</p> <p>Use Enter for one line down, Space for one page down, <code>g</code> for the top, <code>G</code> for the bottom, <code>h</code> or <code>H</code> for help, <code>q</code> to quit, or arrow keys to move through a manual page.</p>"},{"location":"cli-environment/#231-getting-help-lab","title":"2.3.1 Getting Help Lab","text":"<p>At the prompt, try <code>man</code>, then <code>man man</code>; scroll through the page, use the listed navigation commands, and press <code>h</code> while viewing a page.</p> <pre><code># Run man without a page argument\nman\n</code></pre> Expected result <pre><code>man: can't set the locale; make sure $LC_* and $LANG are correct\nWhat manual page do you want?\nFor example, try 'man man'.\n</code></pre> <pre><code># Open the man manual page\nman man\n</code></pre> Expected result <p>The page was opened in the terminal and the help interaction was attempted before normal exit.</p> <pre><code>man: can't set the locale; make sure $LC_* and $LANG are correct\nman: can't resolve man7/groff_man.7\n</code></pre> <pre><code># Open the ls manual page\nman ls\n</code></pre> Expected result <p><code>man</code> opened the page in the terminal and exited normally after the requested navigation keys.</p> <pre><code>man: can't set the locale; make sure $LC_* and $LANG are correct\n</code></pre> <p>Then find the <code>rsync -p</code> option.</p> <pre><code># Open the rsync manual page\nman rsync\n</code></pre> Expected result <p><code>man</code> opened the page in the terminal and exited normally after <code>q</code>.</p> <pre><code>man: can't set the locale; make sure $LC_* and $LANG are correct\n</code></pre> <p>End of the lab. Do not continue to the next topic.</p>"},{"location":"cli-environment/#24-shell-environment-variables","title":"2.4 Shell Environment Variables","text":"<p>A shell environment variable is a named character string that stores information such as numbers, text, filenames, devices, or credentials. <code>PATH</code> tells the shell which directories to search for executable files. Use <code>env</code> or <code>printenv</code> to list defined environment variables. Common variables are <code>SHELL</code> for the default shell, <code>USER</code> for the logged-in user, <code>PWD</code> for the current directory, and <code>HOME</code> for the current user's home directory.</p> <p>To run a command outside <code>PATH</code>, precede it with a relative or full path, such as <code>. /mycommand.sh</code> or <code>/home/myuser/mycommand.sh</code>. Replace the example paths with a known script before running either form.</p>"},{"location":"cli-environment/#naming-variables","title":"Naming Variables","text":"<p>By convention, variable names are uppercase. They can use letters, numbers, and underscores, but cannot begin with a number.</p> <pre><code># Export a terminal-type variable\nexport TERM=xterm-256color\n</code></pre> Expected result <p>No output.</p>"},{"location":"cli-environment/#setting-and-unsetting-variables","title":"Setting and Unsetting Variables","text":"<p>Define variables with <code>name=value</code>; they are local until the shell exits. Use <code>unset</code> or an empty value to undefine them. Use <code>export</code> to make a variable available to child processes.</p> <pre><code># Set local variables\nNAME=Marvin\n</code></pre> Expected result <p>No output.</p> <pre><code># Set a variable containing multiple values\nNAMES=\"Marvin Ford Arthur Trillian\"\n</code></pre> Expected result <p>No output.</p> <pre><code># Unset NAME\nunset NAME\n</code></pre> Expected result <p>No output.</p> <pre><code># Set NAME to an empty value\nNAME=\n</code></pre> Expected result <p>No output.</p> <pre><code># Export NAME to child processes\nexport NAME=Marvin\n</code></pre> Expected result <p>No output.</p>"},{"location":"cli-environment/#manipulating-variables","title":"Manipulating Variables","text":"<pre><code># Display NAME\necho $NAME\n</code></pre> Expected result <pre><code>Marvin\n</code></pre> <pre><code># Append a value to NAMES\nNAMES=\"$NAMES Zaphod\"\n</code></pre> Expected result <p>No output.</p> <pre><code># Mark NAMES as read-only\nreadonly NAMES\n</code></pre> Expected result <p>No output.</p>"},{"location":"cli-environment/#special-variables","title":"Special Variables","text":"<p>Special variables can be referenced but not assigned. <code>$?</code> is the exit status of the last command, <code>$$</code> is the PID of the current shell, and <code>$!</code> is the PID of the last background command. Use them as part of another command, for example <code>echo $?</code>; entering a parameter token alone makes the expanded value a command name.</p>"},{"location":"cli-environment/#241-shell-environment-and-variables-lab","title":"2.4.1 Shell Environment and Variables Lab","text":"<ol> <li>View <code>PATH</code> and environment variables.</li> </ol> <pre><code># Display the PATH variable\necho $PATH\n</code></pre> Expected result <pre><code>/usr/local/sbin:/usr/local/bin:/usr/sbin:/usr/bin:/sbin:/bin:/usr/games:/usr/local/games:/snap/bin\n</code></pre> <pre><code># List environment variables\nenv\n</code></pre> Expected result <pre><code>SHELL=/bin/bash\nNAME=Marvin\nPWD=/home/ubuntu\nHOME=/home/ubuntu\nTERM=xterm-256color\nUSER=ubuntu\n</code></pre> <p>The environment also includes session-specific variables and locale assignments.</p> <ol> <li>Consider the purpose of <code>SHELL</code>. Set and view <code>MYNAME</code>.</li> </ol> <pre><code># Set MYNAME\nMYNAME=\"john\"\n</code></pre> Expected result <p>No output.</p> <pre><code># Display MYNAME\necho $MYNAME\n</code></pre> Expected result <pre><code>john\n</code></pre> <ol> <li>Set <code>PART1</code> and <code>PART2</code>, then display both.</li> </ol> <pre><code># Set the first name fragment\nPART1=\"My name is\"\n</code></pre> Expected result <p>No output.</p> <pre><code># Set the second name fragment\nPART2=\"john\"\n</code></pre> Expected result <p>No output.</p> <pre><code># Display the first fragment\necho $PART1\n</code></pre> Expected result <pre><code>My name is\n</code></pre> <pre><code># Display the second fragment\necho $PART2\n</code></pre> Expected result <pre><code>john\n</code></pre> <ol> <li>Combine the variables into <code>STUDENTNAME</code> and display it.</li> </ol> <pre><code># Combine the name fragments\nSTUDENTNAME=$PART1$PART2\n</code></pre> Expected result <p>No output.</p> <pre><code># Display STUDENTNAME\necho $STUDENTNAME\n</code></pre> Expected result <pre><code>My name isjohn\n</code></pre> <ol> <li>Undefine the variables, then confirm they are empty.</li> </ol> <pre><code># Unset PART1\nunset PART1\n</code></pre> Expected result <p>No output.</p> <pre><code># Unset PART2\nunset PART2\n</code></pre> Expected result <p>No output.</p> <pre><code># Unset STUDENTNAME\nunset STUDENTNAME\n</code></pre> Expected result <p>No output.</p> <pre><code># Confirm PART1 is undefined\necho $PART1\n</code></pre> Expected result <p>No output.</p> <pre><code># Confirm PART2 is undefined\necho $PART2\n</code></pre> Expected result <p>No output.</p> <pre><code># Confirm STUDENTNAME is undefined\necho $STUDENTNAME\n</code></pre> Expected result <p>No output.</p> <ol> <li>Set an unexported variable, create a subshell, and inspect it.</li> </ol> <pre><code># Set x in the current shell\nx=5\n</code></pre> Expected result <p>No output.</p> <pre><code># Display x in the current shell\necho $x\n</code></pre> Expected result <pre><code>5\n</code></pre> <pre><code># Create a subshell\nbash\n</code></pre> Expected result <pre><code>ubuntu@ubuntu:~$\n</code></pre> <pre><code># Display x in the subshell\necho $x\n</code></pre> Expected result <p>No output.</p> <pre><code># Exit back to the original shell\nexit\n</code></pre> Expected result <p>No output.</p> <ol> <li>Export <code>x</code> and inspect it in a subshell.</li> </ol> <pre><code># Export x\nexport x=5\n</code></pre> Expected result <p>No output.</p> <pre><code># Display exported x\necho $x\n</code></pre> Expected result <pre><code>5\n</code></pre> <pre><code># Create a subshell\nbash\n</code></pre> Expected result <pre><code>ubuntu@ubuntu:~$ exit\n</code></pre> <pre><code># Display x in the subshell\necho $x\n</code></pre> Expected result <pre><code>5\n</code></pre> <pre><code># Exit the subshell\nexit\n</code></pre> Expected result <p>No output.</p> <p>End of the lab. Do not continue to the next topic.</p>"},{"location":"cli-environment/#25-standard-streams","title":"2.5 Standard Streams","text":"<p>In computer programming, standard streams are preconnected input and output communication channels between a computer program and its environment when it begins execution. The three I/O connections are standard input (STDIN), standard output (STDOUT), and standard error (STDERR).</p>"},{"location":"cli-environment/#redirection","title":"Redirection","text":"<p>Commands normally take input from the terminal or STDIN through the keyboard. Commands normally send output back to the terminal or STDOUT through the screen. I/O redirection allows commands to receive input from other files or commands, and to send output to files or other commands.</p>"},{"location":"cli-environment/#output-redirection","title":"Output Redirection","text":"<p>The output of a command can be sent to a file with <code>&gt;</code> followed by a filename. If the filename does not exist, it is created. If it exists, it is overwritten. Use two greater-than characters (<code>&gt;&gt;</code>) followed by a filename to append command output to an existing file.</p> <pre><code># Write a detailed listing to a file\nls -al &gt; ~/all_files.txt\n</code></pre> Expected result <p>No output.</p> <pre><code># Append a /tmp listing to a file\nls -al /tmp &gt;&gt; ~/all_files.txt\n</code></pre> Expected result <p>No output.</p> <pre><code># Combine file contents into one file\ncat file1.txt file2.txt file3.txt &gt; combined-file.txt\n</code></pre> Expected result <pre><code>cat: file1.txt: No such file or directory\ncat: file2.txt: No such file or directory\ncat: file3.txt: No such file or directory\n</code></pre> <pre><code># Discard ping output\nping -c 2 www.xxx.yyy.zzz &gt; /dev/null\n</code></pre> Expected result <pre><code>ping: www.xxx.yyy.zzz: Name or service not known\n</code></pre>"},{"location":"cli-environment/#input-redirection","title":"Input Redirection","text":"<p>A command can receive input from a file with <code>&lt;</code> followed by a filename. When two less-than characters (<code>&lt;&lt;</code>) are used, a delimiter must be defined immediately after <code>&lt;&lt;</code>. For example, <code>wc -l &lt;&lt; EOF</code> starts a here-document; the shell reads input until it reaches a line containing that delimiter.</p> <pre><code># Count lines from the passwd file\nwc -l &lt; /etc/passwd\n</code></pre> Expected result <pre><code>33\n</code></pre> <p>This counts lines one at a time until it reaches the <code>EOF</code> delimiter.</p>"},{"location":"cli-environment/#piped-redirection","title":"Piped Redirection","text":"<p>The output of one command can be sent directly to another command as input with <code>|</code>. This is called a pipe or piping.</p> <pre><code># Page through a detailed listing\nls -al | less\n</code></pre> Expected result <pre><code>-rw-rw-r--  1 ubuntu ubuntu 387636 Aug 25 07:23 all_files.txt\n-rw-rw-r--  1 ubuntu ubuntu      0 Aug 25 07:23 combined-file.txt\ndrwxrwxr-x  2 ubuntu ubuntu   4096 Aug 25 07:10 mydir1\n</code></pre> <pre><code># Filter a detailed listing\nls -al | grep string\n</code></pre> Expected result <p>No output.</p>"},{"location":"cli-environment/#advanced-redirection","title":"Advanced Redirection","text":"<p>The three I/O types have their own identifiers, called file descriptors:</p> <ul> <li><code>0</code>: Standard Input (STDIN), data fed into the program.</li> <li><code>1</code>: Standard Output (STDOUT), data printed by the program, which defaults to the terminal.</li> <li><code>2</code>: Standard Error (STDERR), error messages, which default to the terminal.</li> </ul> <p>These descriptors can redirect a data stream from its normal destination.</p> <pre><code># Write normal output and errors to separate files\nls -al /r* &gt; /tmp/list.txt 2&gt;/tmp/errors.txt\n</code></pre> Expected result <p>No output.</p> <pre><code># Write normal output and errors to one file\nls -al /r* &gt; /tmp/list.txt 2&gt;&amp;1\n</code></pre> Expected result <p>No output.</p>"},{"location":"cli-environment/#251-redirection-lab","title":"2.5.1 Redirection Lab","text":"<pre><code># Save a directory listing to mylist\nls &gt; mylist\n</code></pre> Expected result <p>No output.</p> <pre><code># Display the fifth item in /usr/bin listing order\nls /usr/bin | head -5 | tail -1\n</code></pre> Expected result <pre><code>aa-exec\n</code></pre> <pre><code># Find list entries in mylist\ngrep list mylist\n</code></pre> Expected result <pre><code>mylist\n</code></pre> <pre><code># Display the newest entries in /usr/bin\nls -lt /usr/bin | head\n</code></pre> Expected result <pre><code>total 118296\n-rwxr-xr-x 1 root root        1622 Jul 31 20:56 acpidbg\n-rwxr-xr-x 1 root root        1622 Jul 31 20:56 cpupower\n</code></pre> <pre><code># Display the largest directory sizes\ndu | sort -nr | head\n</code></pre> Expected result <pre><code>1016    .\n16  ./.local\n12  ./.local/share\n</code></pre> <pre><code># Create redirection.txt with a here-document\ncat &gt;redirection.txt &lt;&lt;EOF\nHello world!\nEOF\n</code></pre> Expected result <p>No output.</p> <pre><code># Find examine references in less help\nless --help | grep -i examine\n</code></pre> Expected result <pre><code>  :e [_\bfile]            Examine a new file.\n  :n                *  Examine the (N-th) next file from the command line.\n</code></pre> <p>End of the lab. Do not continue to the next topic.</p>"},{"location":"cli-environment/#26-command-chaining","title":"2.6 Command Chaining","text":"<p>Command chaining combines commands on one command line. Operators include <code>&amp;</code> for background execution, <code>;</code> for sequential execution, <code>&amp;&amp;</code> for execution after success, <code>||</code> for execution after failure, <code>!</code> for negation, <code>|</code> for piping, <code>{}</code> for grouping, <code>()</code> for precedence, and <code>\\</code> to continue a command or escape special characters.</p> <pre><code># Run three commands sequentially\nls; pwd; whoami\n</code></pre> Expected result <pre><code>2014-00   Dp-4.txt  Hi-7.txt  Lc-0.txt  Ov-3.txt  So-6.txt  Wh-9.txt\n2014-01   Dp-5.txt  Hi-8.txt  Lc-1.txt  Ov-4.txt  So-7.txt  Wi-0.txt\n2014-02   Dp-6.txt  Hi-9.txt  Lc-2.txt  Ov-5.txt  So-8.txt  Wi-1.txt\n2014-03   Dp-7.txt  Hj-0.txt  Lc-3.txt  Ov-6.txt  So-9.txt  Wi-2.txt\n2014-04   Dp-8.txt  Hj-1.txt  Lc-4.txt  Ov-7.txt  Sp-0.txt  Wi-3.txt\n2014-05   Dp-9.txt  Hj-2.txt  Lc-5.txt  Ov-8.txt  Sp-1.txt  Wi-4.txt\n2014-06   Dq-0.txt  Hj-3.txt  Lc-6.txt  Ov-9.txt  Sp-2.txt  Wi-5.txt\n2014-07   Dq-1.txt  Hj-4.txt  Lc-7.txt  Ow-0.txt  Sp-3.txt  Wi-6.txt\n</code></pre> <p>The complete listing is long and changes as lab files are created; it was followed by <code>/home/ubuntu</code> and <code>ubuntu</code>.</p> <pre><code># Report whether a host can be pinged\nping -c1 google.com &amp;&amp; echo \"That's good, able to ping google.com\" || \\\necho \"That's bad unable to ping google.com\"\n</code></pre> Expected result <pre><code>PING google.com (142.251.110.139) 56(84) bytes of data.\n1 packets transmitted, 1 received, 0% packet loss, time 0ms\nThat's good, able to ping google.com\n</code></pre>"},{"location":"cli-environment/#27-return-values-and-expansion","title":"2.7 Return Values and Expansion","text":"<p>Successful commands return exit code <code>0</code>; unsuccessful commands should return a non-zero code. <code>$?</code> stores the last command's exit status. The shell resolves tokens through expansions, including brace, tilde, parameter, command, arithmetic, and wildcard expansion.</p>"},{"location":"cli-environment/#brace-expansion","title":"Brace Expansion","text":"<p>Brace expansion generates strings from comma-separated values or ranges and can be nested.</p> <pre><code># Demonstrate brace expansion\necho sp{el,il,al}\n</code></pre> Expected result <pre><code>spel spil spal\n</code></pre> <pre><code># Create directories using brace ranges\nmkdir {2014..2016}-0{0..9} {2014..2016}-{10..12}\n</code></pre> Expected result <pre><code>mkdir: cannot create directory '2014-00': File exists\nmkdir: cannot create directory '2014-01': File exists\nmkdir: cannot create directory '2014-02': File exists\n</code></pre> <p>The remaining requested directories already existed and produced the same error.</p> <pre><code># Create files using brace ranges\ntouch file-{A..Z}{a..z}-{0..9}\n</code></pre> Expected result <p>No output.</p> <pre><code># Create text files using brace ranges\ntouch {A..Z}{a..z}-{0..9}.txt\n</code></pre> Expected result <p>No output.</p> <pre><code># Remove files with selected extensions\nrm filename.{pl,sh,py,c}\n</code></pre> Expected result <pre><code>rm: cannot remove 'filename.pl': No such file or directory\nrm: cannot remove 'filename.sh': No such file or directory\nrm: cannot remove 'filename.py': No such file or directory\nrm: cannot remove 'filename.c': No such file or directory\n</code></pre>"},{"location":"cli-environment/#tilde-expansion","title":"Tilde Expansion","text":"<p>Tilde expansion can resolve home directories, the current working directory, and the previous working directory. It occurs only at the beginning of a word or as a separate word.</p> <pre><code># Change to the current user's home directory\ncd ~\n</code></pre> Expected result <p>No output.</p> <pre><code># Change to ubuntu's home directory\ncd ~ubuntu\n</code></pre> Expected result <p>No output.</p> <pre><code># Create hello.txt in the current user's home directory\necho hello &gt;~/hello.txt\n</code></pre> Expected result <p>No output.</p>"},{"location":"cli-environment/#shell-parameter-and-variable-expansion","title":"Shell Parameter and Variable Expansion","text":"<p><code>$</code> introduces parameter expansion, command substitution, or arithmetic expansion. Braces protect a variable name from adjacent characters. <code>${!name}</code> performs indirect expansion, and a default can be assigned while expanding an unset variable.</p> <pre><code># Display the configured shell\necho $SHELL\n</code></pre> Expected result <pre><code>/bin/bash\n</code></pre> <pre><code># Store the current date in D\nD=`date`\n</code></pre> Expected result <p>No output.</p> <pre><code># Display D\necho $D\n</code></pre> Expected result <pre><code>Tue Aug 25 07:29:19 UTC 2026\n</code></pre> <pre><code># Compare braced and unbraced variable expansion\necho ${D}d vs echo $Dd\n</code></pre> Expected result <pre><code>Tue Aug 25 07:29:19 UTC 2026d vs echo\n</code></pre> <p>The environment-dependent pattern <code>echo ${!LC*} vs echo $LC*</code> compares variable names selected by indirect expansion with their expanded locale values. Its result depends on the session locale configuration.</p> <pre><code># Set TODAY during expansion when it is unset\necho ${TODAY:=`date +%A`}\n</code></pre> Expected result <pre><code>Tuesday\n</code></pre>"},{"location":"cli-environment/#command-substitution","title":"Command Substitution","text":"<p>Command substitution replaces a command with its standard output. The forms are <code>$(command)</code> and backticks; trailing newlines are removed.</p> <pre><code># Display the date using command substitution\necho \"The date and time is: $(date)\"\n</code></pre> Expected result <pre><code>The date and time is: Tue Aug 25 07:32:38 UTC 2026\n</code></pre> <pre><code># Display the date using backticks\necho \"The date and time is `date`\"\n</code></pre> Expected result <pre><code>The date and time is Tue Aug 25 07:32:38 UTC 2026\n</code></pre> <pre><code># Display locale environment assignments\necho $(env | grep ^LC_)\n</code></pre> Expected result <pre><code>LC_ADDRESS=ro_RO.UTF-8 LC_NAME=ro_RO.UTF-8 LC_MONETARY=ro_RO.UTF-8 LC_PAPER=ro_RO.UTF-8 LC_IDENTIFICATION=ro_RO.UTF-8 LC_TELEPHONE=ro_RO.UTF-8 LC_MEASUREMENT=ro_RO.UTF-8 LC_NUMERIC=ro_RO.UTF-8\n</code></pre> <pre><code># Display locale assignments one per line\nprintf \"%s\\n\" $(env | grep ^LC_)\n</code></pre> Expected result <pre><code>LC_ADDRESS=ro_RO.UTF-8\nLC_NAME=ro_RO.UTF-8\nLC_MONETARY=ro_RO.UTF-8\nLC_PAPER=ro_RO.UTF-8\nLC_IDENTIFICATION=ro_RO.UTF-8\nLC_TELEPHONE=ro_RO.UTF-8\nLC_MEASUREMENT=ro_RO.UTF-8\nLC_NUMERIC=ro_RO.UTF-8\n</code></pre>"},{"location":"cli-environment/#arithmetic-expansion","title":"Arithmetic Expansion","text":"<p>Arithmetic expansion evaluates an arithmetic expression and substitutes the result. It works with integers. All tokens in the expression undergo parameter expansion, command substitution, and quote removal. Variables within arithmetic expansion can, in some cases, be used with or without <code>$</code>.</p> Operator Meaning <code>+</code> Addition <code>-</code> Subtraction <code>++</code> Increment by 1 <code>--</code> Decrement by 1 <code>+=</code> Addition assignment <code>-=</code> Subtraction assignment <code>*</code> Multiplication <code>/</code> Division <code>*=</code> Multiplication assignment <code>/=</code> Division assignment <code>%=</code> Remainder assignment <code>%</code> Remainder <code>!=</code> Inequality evaluation <code>==</code> Equality evaluation <code>&gt;</code> Greater-than comparison <code>&lt;</code> Less-than comparison <code>&gt;=</code> Greater-than or equal comparison <code>&lt;=</code> Less-than or equal comparison <code>,</code> Expression separator <code>**</code> Exponentiation"},{"location":"cli-environment/#wildcard-expansion","title":"Wildcard Expansion","text":"<p>The shell provides special characters to specify groups of filenames. These characters are called wildcards. Wildcards select filenames or directories based on character patterns and can be used with any command that accepts filename arguments.</p> Wildcard Meaning <code>*</code> Matches any one or more characters <code>?</code> Matches any single character <code>[...]</code> Matches any one of the enclosed characters <code>[[:alnum:]]</code> Matches any alphanumeric character <code>[[:alpha:]]</code> Matches any alphabetic character <code>[[:digit:]]</code> Matches any numeric character <code>[[:upper:]]</code> Matches any uppercase alpha character <code>[[:lower:]]</code> Matches any lowercase alpha character <code>[![:alnum:]]</code> Matches any non-alphanumeric character <code>[![:alpha:]]</code> Matches any non-alphabetic character <code>[![:digit:]]</code> Matches any non-numeric character <code>[![:upper:]]</code> Matches any non-uppercase alpha character <code>[![:lower:]]</code> Matches any non-lowercase alpha character <p>Wildcard listing forms demonstrate pattern syntax. Run them only in a directory with a manageable set of files:</p> <ul> <li><code>ls -ld *</code> lists metadata for all entries.</li> <li><code>ls -ld *.log</code> lists metadata for log files.</li> <li><code>ls -ld [abc]*</code> lists metadata for names starting with <code>a</code>, <code>b</code>, or <code>c</code>.</li> <li><code>ls -ld *.??</code> lists names with a two-character suffix.</li> <li><code>ls -ld *.log.[[:digit:]]</code> lists log names ending with a digit extension.</li> <li><code>ls -ld *.[[:digit:]].??</code> lists names with digit and two-character extensions.</li> <li><code>ls -ld *[![:alnum:]]</code> lists names ending in a non-alphanumeric character.</li> </ul>"},{"location":"cli-environment/#271-chaining-and-expansion-lab","title":"2.7.1 Chaining and Expansion Lab","text":"<p>Try these chaining examples in <code>/tmp</code>; the fallback examples demonstrate <code>||</code>.</p> <pre><code># Change to /tmp, then list it\ncd /tmp; ls -al\n</code></pre> Expected result <pre><code>total 64\ndrwxrwxrwt 14 root root 4096 Aug 25 07:29 .\ndrwxrwxr-x  2 ubuntu ubuntu 4096 Aug 25 07:05 chapter2-validation\n</code></pre> <pre><code># List /tmp only after changing to it\ncd /tmp &amp;&amp; ls -al\n</code></pre> Expected result <pre><code>total 64\ndrwxrwxrwt 14 root root 4096 Aug 25 07:29 .\n</code></pre> <pre><code># Report a failed directory change\ncd /tmp || echo \"Could not change to /tmp\"\n</code></pre> Expected result <p>No output.</p> <pre><code># List /tmp or report failure\ncd /tmp &amp;&amp; ls -al || echo \"Could not change to /tmp\"\n</code></pre> Expected result <pre><code>total 64\ndrwxrwxrwt 14 root root 4096 Aug 25 07:29 .\n</code></pre> <pre><code># Return home after a failed directory change\ncd /tmp &amp;&amp; ls -al || { echo \"Could not change to /tmp. Going home instead\"; cd ~; pwd; }\n</code></pre> Expected result <pre><code>total 64\ndrwxrwxrwt 14 root root 4096 Aug 25 07:29 .\n</code></pre> <pre><code># List /tmp or count entries in /tmp\n(cd /tmp &amp;&amp; ls -al) || (cd /tmp &amp;&amp; { ls -al | wc -l; })\n</code></pre> Expected result <pre><code>total 64\ndrwxrwxrwt 14 root root 4096 Aug 25 07:29 .\n</code></pre> <p>The extglob form <code>ls -ld /var/log/!(*.gz)</code> lists uncompressed log files when extended globbing is enabled. Its output varies by host log retention.</p> <pre><code># List the last ten entries in /etc sort order\nls -ltr /etc | tail\n</code></pre> Expected result <pre><code>-rw-r--r-- 1 root root       1716 Feb 19  2025 passwd\n-rw-r--r-- 1 root root      22103 Aug 25 06:14 ld.so.cache\n</code></pre> <pre><code># Find ls commands in shell history\nhistory | grep ls\n</code></pre> Expected result <p>No output.</p> <pre><code># Set a filename variable\nfile=data.txt\n</code></pre> Expected result <p>No output.</p> <pre><code># Remove the shortest suffix beginning with a period\necho ${file%.*}\n</code></pre> Expected result <pre><code>data\n</code></pre> <pre><code># Remove the shortest prefix ending with a period\necho ${file#*.}\n</code></pre> Expected result <pre><code>txt\n</code></pre> <pre><code># Set X for arithmetic examples\nX=5\n</code></pre> Expected result <p>No output.</p> <pre><code># Add one to X\necho $((X+1))\n</code></pre> Expected result <pre><code>6\n</code></pre> <pre><code># Add one to X with an explicit variable expansion\necho $(($X+1))\n</code></pre> Expected result <pre><code>6\n</code></pre> <pre><code># Compare X with 15\necho $((X&gt;15))\n</code></pre> Expected result <pre><code>0\n</code></pre> <pre><code># Change to the command-binary directory\ncd /usr/bin\n</code></pre> Expected result <p>No output.</p> <p>The glob forms <code>ls m*</code> and <code>ls *.???</code> demonstrate prefix and fixed-length-suffix matching in <code>/usr/bin</code>; their results vary with installed packages.</p> <pre><code># Return to the home directory\ncd ~\n</code></pre> Expected result <p>No output.</p> <p>End of the lab. Do not continue to the next topic.</p>"},{"location":"cli-environment/#28-introduction-to-nano","title":"2.8 Introduction to nano","text":"<p><code>nano</code> is a small, easy-to-learn text editor with interactive search and replace, go-to line and column, auto-indentation, feature toggles, internationalization, and filename tab completion. It is installed by default in Ubuntu and works with <code>sudo</code>.</p> <pre><code># Start nano for a new file\nnano\n</code></pre> Expected result <pre><code>GNU nano 7.2                    New Buffer\n[ Welcome to nano.  For basic help, type Ctrl+G. ]\n^G Help  ^O Write Out  ^W Where Is  ^X Exit\n</code></pre> <p>To edit an existing file, replace the placeholder in <code>nano /path/filename</code> with its actual path.</p> <p>Nano displays commands in its editor window. Press <code>Ctrl+g</code> for additional commands.</p>"},{"location":"cli-environment/#281-nano-lab","title":"2.8.1 nano Lab","text":"<p>Create and edit files with nano, reviewing the commands available in the editor.</p> <pre><code># Change to the home directory\ncd ~\n</code></pre> Expected result <p>No output.</p> <pre><code># Create and edit fileone.txt\nnano fileone.txt\n</code></pre> Expected result <pre><code>GNU nano 7.2                    fileone.txt\n[ New File ]\n^G Help  ^O Write Out  ^W Where Is  ^X Exit\n</code></pre> <p>Type <code>The quick brown fox jumped over the lazy dog.</code>. Copy and paste it ten times using the nano shortcuts:</p> <pre><code>Ctrl+K\nCtrl+U\n</code></pre> <p>Practice moving through the file, then save and close it:</p> <pre><code>Ctrl+X\n</code></pre> <p>Create <code>shells.txt</code>:</p> <pre><code># Create and edit shells.txt\nnano shells.txt\n</code></pre> Expected result <pre><code>GNU nano 7.2                    shells.txt\n[ New File ]\n^G Help  ^O Write Out  ^W Where Is  ^X Exit\n</code></pre> <p>Add <code>She sells seashells down by the seashore.</code>, copy and paste the line three times, then save and close the file. Open the original file, indent the first three lines, and replace <code>fox</code> with <code>rabbit</code>:</p> <pre><code>Ctrl+\\\\\n</code></pre> <p>Move forward one word:</p> <pre><code>Ctrl+Space\n</code></pre> <p>Delete a line:</p> <pre><code>Ctrl+K\n</code></pre> <p>Save and exit:</p> <pre><code>Ctrl+X\n</code></pre> <p>End of the lab. Do not continue to the next topic.</p>"},{"location":"identity-and-ownership/","title":"4. Identity and Ownership","text":"<p>Note</p> <p>Structured from the source material. Command validation is pending.</p> <p>Abstract</p> <p>Manage users, groups, privilege delegation, ownership, and permissions.</p>"},{"location":"identity-and-ownership/#41-user-management","title":"4.1 User Management","text":"<p>User accounts control system access through a unique login and password, permissions, and logging. Account information is stored in <code>/etc/passwd</code> and <code>/etc/shadow</code>; encrypted passwords cannot be read directly.</p> Command Purpose <code>adduser</code> Create a user account. <code>usermod</code> Modify a user account. <code>passwd</code> Change a password. <code>deluser</code> Delete a user account. <p>Permissions on files and directories provide read, write, and execute access to users or groups.</p> <pre><code># Inspect file permissions\nls -l file.txt\n</code></pre> Expected result <p>Validation pending; no captured output is available.</p> <pre><code># Edit the sudoers policy safely\nsudo visudo /etc/sudoers\n</code></pre> Expected result <p>Validation pending; no captured output is available.</p> <pre><code># Add a limited sudoers command rule\nubuntu ALL=(ALL) : /usr/bin/command\n</code></pre> Expected result <p>Validation pending; no captured output is available.</p> <pre><code># Add a password-free sudoers command rule\nubuntu ALL=(ALL) NOPASSWD: /usr/bin/command arg1 arg2\n</code></pre> Expected result <p>Validation pending; no captured output is available.</p> <pre><code># Create the students group\nsudo addgroup students\n</code></pre> Expected result <p>Validation pending; no captured output is available.</p> <pre><code># Verify the group entry\ngrep students /etc/group\n</code></pre> Expected result <p>Validation pending; no captured output is available.</p> <pre><code># Create labuser with students as its primary group\nsudo adduser --ingroup students --disabled-password labuser\n</code></pre> Expected result <p>Validation pending; no captured output is available.</p> <pre><code># Verify the passwd entry\ngrep labuser /etc/passwd\n</code></pre> Expected result <p>Validation pending; no captured output is available.</p> <pre><code># Inspect the shadow entry\nsudo grep labuser /etc/shadow\n</code></pre> Expected result <p>Validation pending; no captured output is available.</p> <pre><code># Set the labuser password\nsudo passwd labuser\n</code></pre> Expected result <p>Validation pending; no captured output is available.</p> <pre><code># Reinspect the shadow entry\nsudo grep labuser /etc/shadow\n</code></pre> Expected result <p>Validation pending; no captured output is available.</p> <pre><code># Add labuser to the sudo group\nsudo adduser labuser sudo\n</code></pre> Expected result <p>Validation pending; no captured output is available.</p> <pre><code># Verify supplementary group membership\ngrep labuser /etc/group\n</code></pre> Expected result <p>Validation pending; no captured output is available.</p> <pre><code># Start a login shell as labuser\nsudo su - labuser\n</code></pre> Expected result <p>Validation pending; no captured output is available.</p> <pre><code># Show the labuser home directory\necho $HOME\n</code></pre> Expected result <p>Validation pending; no captured output is available.</p> <pre><code># Leave the labuser shell\nexit\n</code></pre> Expected result <p>Validation pending; no captured output is available.</p> <pre><code># Remove labuser and its home directory\nsudo userdel -r labuser\n</code></pre> Expected result <p>Validation pending; no captured output is available.</p> <pre><code># Start a root login shell\nsudo -i\n</code></pre> Expected result <p>Validation pending; no captured output is available.</p> <pre><code># Leave the root shell\nexit\n</code></pre> Expected result <p>Validation pending; no captured output is available.</p> <pre><code># Inspect a detailed directory listing\nls -l\n</code></pre> Expected result <p>Validation pending; no captured output is available.</p> <pre><code># Change a file owner and group\nchown username:group filename\n</code></pre> Expected result <p>Validation pending; no captured output is available.</p> <pre><code># Remove group write access\nchmod g-w filename\n</code></pre> Expected result <p>Validation pending; no captured output is available.</p> <pre><code># Remove other write access\nchmod o-w filename\n</code></pre> Expected result <p>Validation pending; no captured output is available.</p> <pre><code># Set mode 755\nchmod 755 filename\n</code></pre> Expected result <p>Validation pending; no captured output is available.</p> <pre><code># Set the default creation mask\numask 037\n</code></pre> Expected result <p>Validation pending; no captured output is available.</p> <pre><code># Grant full access with numeric mode\nchmod 777 filename\n</code></pre> Expected result <p>Validation pending; no captured output is available.</p> <pre><code># Grant full access with symbolic mode\nchmod uog+rwx filename\n</code></pre> Expected result <p>Validation pending; no captured output is available.</p> <pre><code># Set a permissive creation mask\numask 000 filename\n</code></pre> Expected result <p>Validation pending; no captured output is available.</p> <pre><code># Restrict access to the owner\nchmod 700 filename\n</code></pre> Expected result <p>Validation pending; no captured output is available.</p> <pre><code># Remove group and other access\nchmod og-rwx filename\n</code></pre> Expected result <p>Validation pending; no captured output is available.</p> <pre><code># Set an owner-only creation mask\numask 077 filename\n</code></pre> Expected result <p>Validation pending; no captured output is available.</p> <pre><code># Add write access for all classes\nchmod ugo+w filename\n</code></pre> Expected result <p>Validation pending; no captured output is available.</p> <pre><code># Create an empty file\ntouch file01.txt\n</code></pre> Expected result <p>Validation pending; no captured output is available.</p> <pre><code># Create a directory\nmkdir test01\n</code></pre> Expected result <p>Validation pending; no captured output is available.</p> <pre><code># Inspect the initial permissions\nls -l\n</code></pre> Expected result <p>Validation pending; no captured output is available.</p> <pre><code># Restrict default permissions to the owner\numask 077\n</code></pre> Expected result <p>Validation pending; no captured output is available.</p> <pre><code># Create a second empty file\ntouch file02.txt\n</code></pre> Expected result <p>Validation pending; no captured output is available.</p> <pre><code># Create a second directory\nmkdir test02\n</code></pre> Expected result <p>Validation pending; no captured output is available.</p> <pre><code># Compare the resulting permissions\nls -l\n</code></pre> Expected result <p>Validation pending; no captured output is available.</p> <pre><code># Match file02 permissions to file01\nchmod 664 file02.txt\n</code></pre> Expected result <p>Validation pending; no captured output is available.</p> <pre><code># Match test02 permissions to test01\nchmod 775 test02\n</code></pre> Expected result <p>Validation pending; no captured output is available.</p> <pre><code># Verify the permission changes\nls -l\n</code></pre> Expected result <p>Validation pending; no captured output is available.</p>"},{"location":"linux-filesystem-hierarchy/","title":"3. Linux Filesystem Hierarchy","text":"<p>Goals</p> <p>Define the Linux Filesystem Hierarchy Standard, explain the Ubuntu filesystem hierarchy, and identify Linux file types.</p> <p>Note</p> <p>All actionable Chapter 3 commands were validated on the LAB HOST. Directory and device listings vary with host state.</p>"},{"location":"linux-filesystem-hierarchy/#31-the-filesystem-hierarchy-standard","title":"3.1 The Filesystem Hierarchy Standard","text":""},{"location":"linux-filesystem-hierarchy/#directory-structure","title":"Directory Structure","text":"<p>The Filesystem Hierarchy Standard (FHS), maintained by the Linux Foundation, defines directory structure and contents for Unix and Unix-like operating systems. It is currently used only by Linux distributions. Under the FHS, all files and directories appear below the root directory <code>/</code>, even when stored on different physical or virtual devices.</p> <p>The root filesystem must be adequate to boot, restore, recover, or repair the system. It should be as small as reasonably possible because it can be mounted from small media, is less prone to corruption after a system crash, and must not contain application-created or application-required files and subdirectories.</p> <p>To boot, the root filesystem must contain enough software and data to mount other filesystems, including utilities, boot-loader configuration, and essential start-up data. To recover or repair a system, it must provide utilities for an experienced maintainer to diagnose and repair issues and reconstruct a damaged system. To restore a system, it must provide backup restoration utilities.</p>"},{"location":"linux-filesystem-hierarchy/#the-cd-command","title":"The <code>cd</code> Command","text":"<p>The <code>cd</code> command changes the current directory. Its reference syntax is <code>cd [option] [directory]</code>.</p> <p>Without a directory name, <code>cd</code> returns the user to the previous current directory, which can toggle between two directories. With a directory name, it changes to that directory. The name can be an absolute pathname, relative to <code>/</code>, or a local pathname, relative to the current directory.</p> <p>Examples using absolute paths include <code>cd /</code> and <code>cd /usr/sbin</code>.</p> <p>To change from the current directory into a subdirectory, use <code>cd</code> followed by its name, for example <code>cd gconf</code> from within <code>/etc</code> when that directory is available.</p> <p>The current directory is represented by <code>.</code>, and its parent directory by <code>..</code>; use <code>cd ..</code> to move to the parent.</p> <p>A tilde returns to the home directory, where a user's personal files, directories, and programs are found; use <code>cd ~</code> to change there.</p> <p>Other navigation forms include <code>cd -</code> to move to the previous directory and <code>cd ../../</code> to move two levels up.</p>"},{"location":"linux-filesystem-hierarchy/#32-required-root-filesystem-directories","title":"3.2 Required Root Filesystem Directories","text":"<p>The following directories, or symbolic links to directories, are required in <code>/</code>:</p> <pre><code>/bin     - essential command binaries\n/dev     - device files\n/etc     - host-specific system configuration\n/sbin    - essential system binaries\n/usr     - secondary hierarchy\n/var     - variable data hierarchy\n/boot    - static files of the boot loader\n/lib     - essential shared libraries and kernel modules\n/media   - mount point for removable media\n/mnt     - mount point for mounting a filesystem temporarily\n/tmp     - temporary files\n</code></pre> <p>Modern Linux distributions also include:</p> <pre><code>/sys     - Kernel system information virtual filesystem\n/proc    - Kernel process information virtual filesystem\n</code></pre> <p>Optional root filesystem directories include:</p> <pre><code>/home                - User home directories\n/lib64 or /lib32     - Essential system libraries\n/root                - Home directory for the root user\n</code></pre>"},{"location":"linux-filesystem-hierarchy/#the-usr-hierarchy","title":"The <code>/usr</code> Hierarchy","text":"<p>The second major filesystem section is shareable and should contain only read-only data. Required subdirectories are:</p> <pre><code>/usr/bin       - Most user commands\n/usr/lib       - Libraries\n/usr/local     - Local hierarchy\n/usr/sbin      - Non-vital system binaries\n/usr/share     - Architecture independent data\n</code></pre> <p>Optional subdirectories include:</p> <pre><code>/usr/games                  - Games and educational binaries\n/usr/include                - Header files included by C programs\n/usr/libexec                - Binaries run by other programs\n/usr/lib64 or /usr/lib32    - Alternate format libraries\n/usr/src                    - Source code\n</code></pre>"},{"location":"linux-filesystem-hierarchy/#the-var-hierarchy","title":"The <code>/var</code> Hierarchy","text":"<p>The <code>/var</code> directory is designed for files that vary in size:</p> <ul> <li>Spool directories and files</li> <li>Administrative and logging data</li> <li>Transient files</li> <li>Temporary files</li> </ul> <p>Required subdirectories are:</p> <pre><code>/var/cache     - Application cache data\n/var/lib       - Variable state information\n/var/local     - Variable data for /usr/local\n/var/lock      - Lock files\n/var/log       - Log files and directories\n/var/opt       - Variable data for /opt\n/var/run       - Data relevant to running processes\n/var/spool     - Application spool data\n/var/tmp       - Temporary files preserved between system reboots\n</code></pre>"},{"location":"linux-filesystem-hierarchy/#the-etc-hierarchy","title":"The <code>/etc</code> Hierarchy","text":"<p>The <code>/etc</code> directory maintains many files, including:</p> <pre><code>/etc/passwd - user database with user information such as username and home directory\n/etc/shadow - encrypted file that holds user passwords\n/etc/fstab - lists filesystems mounted automatically at startup by mount -a\n/etc/group - similar to /etc/passwd, but describes groups instead of users\n/etc/inittab - configuration file for init\n/etc/mtab - list of currently mounted filesystems\n/etc/profile, /etc/bash.rc - files executed by BASH at login or startup\n</code></pre>"},{"location":"linux-filesystem-hierarchy/#321-directory-structure-lab","title":"3.2.1 Directory Structure Lab","text":"<p>This lab examines filesystem directories and their contents.</p> <ol> <li>Check the current directory, then change to the root directory.</li> </ol> <pre><code># Print the current working directory.\npwd\n</code></pre> Expected result <p><code>/home/ubuntu</code></p> <pre><code># Change to the root directory.\ncd /\n</code></pre> Expected result <p>No output.</p> <ol> <li>List the root directory contents and compare them with the preceding directory list.</li> </ol> <pre><code># List the root directory contents.\nls\n</code></pre> Expected result <pre><code>bin                 etc                lib64       opt   sbin                  sys\nbin.usr-is-merged   home               lost+found  proc  sbin.usr-is-merged    tmp\nboot                lib                media       root  snap                  usr\ndev                 lib.usr-is-merged  mnt         run   srv                   var\n</code></pre> <ol> <li>Change into <code>/usr</code>.</li> </ol> <pre><code># Change to the /usr directory.\ncd /usr\n</code></pre> Expected result <p>No output.</p> <ol> <li>Inspect the listed <code>usr</code> subdirectories.</li> </ol> <pre><code># List the /usr/lib directory contents.\nls lib\n</code></pre> Expected result <p>Literal excerpt from this LAB HOST; installed packages affect this listing:</p> <pre><code>apparmor\napt\nbinfmt.d\nbyobu\ncloud-init\ncommand-not-found\nconsole-setup\ncryptsetup\ndbus-1.0\ndpkg\n</code></pre> <pre><code># List the /usr/games directory contents.\nls games\n</code></pre> Expected result <p>No output.</p> <pre><code># List the /usr/local directory contents.\nls local\n</code></pre> Expected result <pre><code>bin  etc  games  include  lib  man  sbin  share  src\n</code></pre> <ol> <li>Change back to <code>/</code>, then change to <code>/var</code>.</li> </ol> <pre><code># Change to the root directory.\ncd /\n</code></pre> Expected result <p>No output.</p> <pre><code># Change to the /var directory.\ncd /var\n</code></pre> Expected result <p>No output.</p> <ol> <li>Inspect the <code>var</code> subdirectory listed on the preceding page.</li> </ol> <pre><code># List the /var/cache directory contents.\nls cache\n</code></pre> Expected result <pre><code>PackageKit  apparmor  fwupd       man        private\nadduser     apt       fwupdmgr    motd-news  snapd\napp-info    debconf   ldconfig    pollinate  swcatalog\n</code></pre> <ol> <li>Change to the <code>cache</code> subdirectory, then move two directories up.</li> </ol> <pre><code># Change to the cache subdirectory.\ncd cache\n</code></pre> Expected result <p>No output.</p> <pre><code># Move two directories up from the current directory.\ncd ../../\n</code></pre> Expected result <p>No output.</p> <ol> <li>Change to the home directory.</li> </ol> <pre><code># Change to the current user's home directory.\ncd ~\n</code></pre> Expected result <p>No output.</p> <ol> <li>Install <code>tree</code>, then run it from the home directory.</li> </ol> <pre><code># Install the tree package without an interactive confirmation prompt.\nsudo apt install tree -y\n</code></pre> Expected result <p>Literal excerpt; package-manager progress and follow-up maintenance output vary by host state:</p> <pre><code>The following NEW packages will be installed:\n  tree\n0 upgraded, 1 newly installed, 0 to remove and 261 not upgraded.\nNeed to get 47.4 kB of archives.\nAfter this operation, 111 kB of additional disk space will be used.\nGet:1 http://archive.ubuntu.com/ubuntu noble-updates/universe amd64 tree amd64 2.1.1-2ubuntu3.24.04.2 [47.4 kB]\nSetting up tree (2.1.1-2ubuntu3.24.04.2) ...\n</code></pre> <pre><code># Change to the current user's home directory.\ncd ~\n</code></pre> Expected result <p>No output.</p> <pre><code># Display the home directory in a tree-like format.\ntree\n</code></pre> Expected result <p>Literal excerpt from this LAB HOST. The full listing contained 13,576 lines and varies with home-directory contents:</p> <pre><code>.\n\u251c\u2500\u2500 2014-00\n\u251c\u2500\u2500 2014-01\n\u251c\u2500\u2500 2014-02\n\u251c\u2500\u2500 2014-03\n\u251c\u2500\u2500 2014-04\n\u251c\u2500\u2500 2014-05\n\u251c\u2500\u2500 2014-06\n\u251c\u2500\u2500 2014-07\n\u251c\u2500\u2500 2014-08\n</code></pre> <ol> <li>List <code>/</code> with <code>tree</code> and inspect it.</li> </ol> <pre><code># Display one level of the root directory tree.\ntree -L 1 /\n</code></pre> Expected result <pre><code>/\n\u251c\u2500\u2500 bin -&gt; usr/bin\n\u251c\u2500\u2500 bin.usr-is-merged\n\u251c\u2500\u2500 boot\n\u251c\u2500\u2500 dev\n\u251c\u2500\u2500 etc\n\u251c\u2500\u2500 home\n\u251c\u2500\u2500 lib -&gt; usr/lib\n\u251c\u2500\u2500 lib.usr-is-merged\n\u251c\u2500\u2500 lib64 -&gt; usr/lib64\n\u251c\u2500\u2500 lost+found\n\u251c\u2500\u2500 media\n\u251c\u2500\u2500 mnt\n\u251c\u2500\u2500 opt\n\u251c\u2500\u2500 proc\n\u251c\u2500\u2500 root\n\u251c\u2500\u2500 run\n\u251c\u2500\u2500 sbin -&gt; usr/sbin\n\u251c\u2500\u2500 sbin.usr-is-merged\n\u251c\u2500\u2500 snap\n\u251c\u2500\u2500 srv\n\u251c\u2500\u2500 sys\n\u251c\u2500\u2500 tmp\n\u251c\u2500\u2500 usr\n\u2514\u2500\u2500 var\n\n25 directories, 0 files\n</code></pre> <ol> <li>Go one level deeper and inspect it.</li> </ol> <pre><code># Display two levels of the root directory tree.\ntree -L 2 /\n</code></pre> Expected result <p>Literal excerpt from this LAB HOST. The full listing contained 696 lines and varies with installed kernels and host state:</p> <pre><code>/\n\u251c\u2500\u2500 bin -&gt; usr/bin\n\u251c\u2500\u2500 bin.usr-is-merged\n\u251c\u2500\u2500 boot\n\u2502\u00a0\u00a0 \u251c\u2500\u2500 System.map-6.8.0-138-generic\n\u2502\u00a0\u00a0 \u251c\u2500\u2500 System.map-6.8.0-53-generic\n\u2502\u00a0\u00a0 \u251c\u2500\u2500 config-6.8.0-138-generic\n\u2502\u00a0\u00a0 \u251c\u2500\u2500 config-6.8.0-53-generic\n\u2502\u00a0\u00a0 \u251c\u2500\u2500 efi\n\u2502\u00a0\u00a0 \u251c\u2500\u2500 grub\n\u2502\u00a0\u00a0 \u251c\u2500\u2500 initrd.img -&gt; initrd.img-6.8.0-138-generic\n\u2502\u00a0\u00a0 \u251c\u2500\u2500 initrd.img-6.8.0-138-generic\n\u2502\u00a0\u00a0 \u251c\u2500\u2500 initrd.img-6.8.0-53-generic\n\u2502\u00a0\u00a0 \u2514\u2500\u2500 initrd.img.old -&gt; initrd.img-6.8.0-53-generic\n</code></pre> <p>End of the lab. Do not continue with the next topic.</p>"},{"location":"linux-filesystem-hierarchy/#33-linux-file-types","title":"3.3 Linux File Types","text":""},{"location":"linux-filesystem-hierarchy/#determining-file-types","title":"Determining File Types","text":"<p>Use <code>ls</code> to determine a file type.</p> <pre><code># List detailed information for /etc/hosts.\nls -l /etc/hosts\n</code></pre> Expected result <pre><code>-rw-r--r-- 1 root root 221 Feb 14  2025 /etc/hosts\n</code></pre> <p>The first character in the output, <code>-</code> in this example, indicates the file type. Linux file types are:</p> Type Symbol Description regular <code>-</code> Any file that is not a directory or special file type. directory <code>d</code> The second-most common file type. socket <code>s</code> Provides inter-process communication. block <code>b</code> A device file that communicates with a driver in the kernel. link <code>l</code> A pointer to another file; can be a soft or hard link. named pipe <code>p</code> Similar to sockets; provides IPC without network sockets. character <code>c</code> A device file that communicates with a driver in the kernel. <p>A <code>regular file</code> is not a directory or special file type. Regular files include binaries or libraries, configuration files, image files, audio or video files, data files, and tar files.</p> <p>A <code>directory</code> is the second-most common file type. It organizes other file types and is essentially a file that lists other files.</p> <p>A <code>socket</code> provides inter-process communication. When it has a filename, it can be accessed through the filesystem. TCP/IP sockets can be accessed over the network.</p> <p>A <code>block file</code> communicates with a kernel driver, which usually communicates with hardware. Data flow is buffered. Block files are used with block storage devices such as disks or memory. Examples include <code>/dev/sda</code> and <code>/dev/ram0</code>.</p> <p>A <code>pipe</code> is similar to a socket and lets processes communicate without network socket semantics; the processes do not need to be designed to work together.</p> <p>A <code>character</code> file communicates with a kernel driver, which usually communicates with hardware. Data flow is unbuffered: each character is consumed when written and read when provided. Keyboard and mouse files are examples. Character special files include <code>/dev/ttyS0</code> and <code>/dev/console</code>.</p>"},{"location":"linux-filesystem-hierarchy/#331-file-types-lab","title":"3.3.1 File Types Lab","text":"<p>This lab examines file types.</p> <ol> <li>Run the following commands and identify the file types.</li> </ol> <pre><code># List detailed information for the /etc directory.\nls -ld /etc\n</code></pre> Expected result <pre><code>drwxr-xr-x 106 root root 4096 Aug 25 06:14 /etc\n</code></pre> <pre><code># List detailed information for /etc/hosts.\nls -l /etc/hosts\n</code></pre> Expected result <pre><code>-rw-r--r-- 1 root root 221 Feb 14  2025 /etc/hosts\n</code></pre> <pre><code># List detailed information for the controlling terminal.\nls -l /dev/tty\n</code></pre> Expected result <pre><code>crw-rw-rw- 1 root tty 5, 0 Aug 25 07:46 /dev/tty\n</code></pre> <pre><code># List detailed information for the vda block device.\nls -l /dev/vda\n</code></pre> Expected result <pre><code>brw-rw---- 1 root disk 253, 0 Aug 25 06:55 /dev/vda\n</code></pre> <ol> <li>Inspect the many file types in <code>/dev</code>.</li> </ol> <pre><code># List detailed information for device files.\nls -l /dev/\n</code></pre> Expected result <p>Literal excerpt from this LAB HOST; device entries and timestamps vary by host state:</p> <pre><code>total 0\ncrw-r--r-- 1 root root     10, 235 Aug 25 06:55 autofs\ndrwxr-xr-x 2 root root         340 Aug 25 06:55 block\ncrw-rw---- 1 root disk     10, 234 Aug 25 06:55 btrfs-control\ndrwxr-xr-x 3 root root          60 Aug 25 06:55 bus\ndrwxr-xr-x 2 root root        3300 Aug 25 06:55 char\ncrw--w---- 1 root tty       5,   1 Aug 25 06:56 console\nlrwxrwxrwx 1 root root          11 Aug 25 06:55 core -&gt; /proc/kcore\n</code></pre> <ol> <li>Create a file and inspect its type.</li> </ol> <pre><code># Create an empty regular file.\ntouch file1.txt\n</code></pre> Expected result <p>No output.</p> <pre><code># List detailed information for the new file.\nls -l file1.txt\n</code></pre> Expected result <pre><code>-rw-rw-r-- 1 ubuntu ubuntu 0 Aug 25 07:50 file1.txt\n</code></pre> <ol> <li>Create a directory and inspect it.</li> </ol> <pre><code># Create a directory named dirone.\nmkdir dirone\n</code></pre> Expected result <p>No output.</p> <pre><code># List the dirone directory entry.\nls -l | grep dirone\n</code></pre> Expected result <pre><code>drwxrwxr-x 2 ubuntu ubuntu 4096 Aug 25 07:50 dirone\n</code></pre> <ol> <li>Remove the file and directory.</li> </ol> <pre><code># Remove the empty dirone directory.\nrmdir dirone/\n</code></pre> Expected result <p>No output.</p> <pre><code># Remove the file1.txt file.\nrm file1.txt\n</code></pre> Expected result <p>No output.</p> <p>End of the lab. Do not continue with the next topic.</p>"},{"location":"logging-and-initialization/","title":"5. Logging and Initialization","text":"<p>Note</p> <p>Structured from the source material. Command validation is pending.</p> <p>Abstract</p> <p>Inspect system logs, understand boot initialization, and manage systemd units.</p>"},{"location":"logging-and-initialization/#51-system-logging","title":"5.1 System Logging","text":"<p>Logging records normal operation, state changes, diagnostics, kernel and network events, authentication, user actions, and exceptions. Logs are a first troubleshooting step and support security monitoring. Ubuntu commonly writes local logs under <code>/var/log/</code> through <code>rsyslogd</code>, including <code>/var/log/kern.log</code>, <code>/var/log/syslog</code>, and <code>/var/log/auth.log</code>. Systems can also send logs to a remote collector.</p> <p>Search a log by source or severity:</p> <pre><code># Search syslog for NetworkManager entries\ngrep NetworkManager /var/log/syslog\n</code></pre> Expected result <p>Validation pending; no captured output is available.</p> <pre><code># Search syslog for errors without case sensitivity\ngrep -i \"error\" /var/log/syslog\n</code></pre> Expected result <p>Validation pending; no captured output is available.</p> <pre><code># List per-application logrotate rules\nls /etc/logrotate.d/\n</code></pre> Expected result <p>Validation pending; no captured output is available.</p> <pre><code># Run log rotation using the main configuration\nsudo logrotate /etc/logrotate.conf\n</code></pre> Expected result <p>Validation pending; no captured output is available.</p> <pre><code># Inspect the main logrotate configuration\ncat /etc/logrotate.conf\n</code></pre> Expected result <p>Validation pending; no captured output is available.</p> <pre><code># Inspect logrotate history\ncat /var/lib/logrotate/status\n</code></pre> Expected result <p>Validation pending; no captured output is available.</p> <pre><code># Force verbose log rotation for troubleshooting\nsudo logrotate -vf /etc/logrotate.conf\n</code></pre> Expected result <p>Validation pending; no captured output is available.</p> <pre><code># Display kernel messages\ndmesg\n</code></pre> Expected result <p>Validation pending; no captured output is available.</p> <pre><code># Page through kernel messages\ndmesg | less\n</code></pre> Expected result <p>Validation pending; no captured output is available.</p> <pre><code># Find vda device messages\ndmesg | grep -i vda\n</code></pre> Expected result <p>Validation pending; no captured output is available.</p> <pre><code># Save vda messages to a file\ndmesg | grep vda &gt; vda.txt\n</code></pre> Expected result <p>Validation pending; no captured output is available.</p> <pre><code># Search syslog for errors\ngrep error /var/log/syslog\n</code></pre> Expected result <p>Validation pending; no captured output is available.</p> <pre><code># Show the first ten syslog lines\nhead -n 10 /var/log/syslog\n</code></pre> Expected result <p>Validation pending; no captured output is available.</p> <pre><code># Show the last ten syslog lines\ntail -n 10 /var/log/syslog\n</code></pre> Expected result <p>Validation pending; no captured output is available.</p> <pre><code># List available log files\nls -l /var/log\n</code></pre> Expected result <p>Validation pending; no captured output is available.</p> <pre><code># List boot files\nls -l /boot/\n</code></pre> Expected result <p>Validation pending; no captured output is available.</p> <pre><code># Read the systemctl manual\nman systemctl\n</code></pre> Expected result <p>Validation pending; no captured output is available.</p> <pre><code># Stop the cron service\nsudo systemctl stop cron\n</code></pre> Expected result <p>Validation pending; no captured output is available.</p> <pre><code># Inspect stopped cron service status\nsudo systemctl status cron\n</code></pre> Expected result <p>Validation pending; no captured output is available.</p> <pre><code># Start the cron service\nsudo systemctl start cron\n</code></pre> Expected result <p>Validation pending; no captured output is available.</p> <pre><code># Inspect running cron service status\nsudo systemctl status cron\n</code></pre> Expected result <p>Validation pending; no captured output is available.</p> <pre><code># List service units\nsystemctl list-units -t service\n</code></pre> Expected result <p>Validation pending; no captured output is available.</p> <pre><code># Show running SSH services\nsystemctl list-units -t service | grep -i ssh\n</code></pre> Expected result <p>Validation pending; no captured output is available.</p> <pre><code># List failed units\nsystemctl --failed\n</code></pre> Expected result <p>Validation pending; no captured output is available.</p> <pre><code># Read the journalctl manual\nman journalctl\n</code></pre> Expected result <p>Validation pending; no captured output is available.</p> <pre><code># Query the SSH journal\njournalctl -u ssh.service\n</code></pre> Expected result <p>Validation pending; no captured output is available.</p> <pre><code># Show SSH service properties\nsystemctl show ssh.service\n</code></pre> Expected result <p>Validation pending; no captured output is available.</p> <pre><code># Restart the SSH service\nsudo systemctl restart ssh\n</code></pre> Expected result <p>Validation pending; no captured output is available.</p>"},{"location":"networking-appendix/","title":"Appendix: Networking","text":"<p>Pending validation</p> <p>This appendix is structured from the source material. Command validation is pending.</p>"},{"location":"networking-appendix/#networking-concepts-tcpip","title":"Networking Concepts - TCP/IP","text":"<p>The Transmission Control Protocol and Internet Protocol (TCP/IP) is a standard set of protocols developed in the late 1970s by the Defense Advanced Research Projects Agency (DARPA) as a means of communication between different types of computers and computer networks. TCP/IP is the driving force of the Internet, and thus it is the most popular set of network protocols on Earth.</p> <p>The two protocol components of TCP/IP deal with different aspects of computer networking. Internet Protocol, the \"IP\" of TCP/IP is a connectionless protocol which deals only with network packet routing using the IP Datagram as the basic unit of networking information. The IP Datagram consists of a header followed by a message. The Transmission Control Protocol, the \"TCP\", enables network hosts to establish connections which may be used to exchange data streams. TCP also guarantees that the data between connections is delivered and arrives in the same order as it was sent.</p> <p>The TCP/IP protocol configuration consists of several elements which must be set by editing the appropriate configuration files or deploying solutions such as the Dynamic Host Configuration Protocol (DHCP) server. The DHCP can be configured to provide the proper TCP/IP configuration settings to network clients automatically. These configuration values must be set correctly to ensure the proper network operation of your Ubuntu system.</p> <p>The common configuration elements of TCP/IP and their purposes are as follows:</p> <ul> <li>Network interfaces are logical constructs that represent a network device. The interface contains the L2 (physical network) and L3 (IPv4 or IPv6 protocol layer) address information, configuration information, and device state.</li> <li>Network devices, frequently called NIC, are physical hardware that connects to a network. Virtual devices emulate a physical network facility in software, (ex. a bond is a virtual device that aggregates together a set of network interfaces.)</li> <li>The neighbour subsystem manages information and configuration for L2 (link layer) destinations.</li> <li>Routing is the act of sending traffic originating on one subnet to a second subnet.</li> <li>The routing layer manages information and configuration for L3 protocol forwarding. This allows L3 subnets to communicate with one another.</li> <li>The IP address is a unique string comprised of 4 decimal numbers ranging from 0 (zero) to 255, separated by periods, with each of the four numbers representing 8 bits of the 32 bit address. This dotted quad notation is obtained through static assignment or dynamically via DHCP.</li> <li>A subnet (for this document) is the extent of the L3 (i.e., IPv4 or IPv6) network segment that delineates the set of destinations to which an IP datagram may be delivered without being routed.</li> <li>The Subnet Mask (or netmask) is a local bit mask, or set of flags which separates the portions of an IP address significant to the network from the bits significant to the subnetwork. The standard netmask 255.255.255.0 masks the first 3 bytes and allows the last byte to remain available to specify hosts on the subnetwork.</li> <li>The Network Address represents the bytes comprising the network portion of an IP address. The host 12.128.1.2 would use 12.0.0.0 as the network address, where 12 represents the first byte (the network part) and zeroes (0) in the remaining 3 bytes represent the potential host values.</li> <li>Gateway is synonymous with router; it is responsible for the connection of two or more subnets by passing L3 protocol traffic between them.</li> <li>The Broadcast Address is an IP address which allows network data to be sent simultaneously to all hosts on a given subnetwork rather than specifying a particular host. For example, on 192.168.1.0, the broadcast address is 192.168.1.255. Broadcast messages are typically produced by network protocols such as the Address Resolution Protocol (ARP) and the Routing Information Protocol (RIP).</li> <li>A Gateway Address is the IP address through which a particular network, or host on a network, may be reached. If one network host wishes to communicate with another network host, and that host is not located on the same network, then a gateway must be used (ex. a router on the same network).</li> <li>Nameserver Addresses represent the IP addresses of Domain Name Service (DNS) systems, which resolve network hostnames into IP addresses. In order for your system to be able to resolve network hostnames into their corresponding IP addresses, you must specify valid Nameserver Addresses which you are authorized to use in your system's TCP/IP configuration. In many cases, these addresses can and will be provided by your network service provider, but many free and publicly accessible nameservers are available.</li> </ul>"},{"location":"networking-appendix/#ip-routing","title":"IP Routing","text":"<p>IP routing is a means of specifying and discovering paths in a TCP/IP network along which network data may be sent. Routing uses a set of routing tables to direct the forwarding of network data packets from their source to the destination, via many intermediary network nodes known as routers. The primary forms are Static Routing and Dynamic Routing.</p> <p><code>Static routing</code> involves manually adding IP routes to the system's routing table with the route command. It has the advantage of simplicity of implementation on smaller networks, predictability (the routing table is always computed in advance so the route is the same each time), and low overhead on other routers and network links due to the lack of a dynamic routing protocol. The disadvantages are that it is limited to small networks and does not scale well. It also fails completely to adapt to network outages and failures along the route due to the fixed nature of the route.</p> <p><code>Dynamic routing</code> depends on large networks with multiple possible IP routes from a source to a destination and makes use of special routing protocols, such as the Router Information Protocol (RIP), which handle the automatic adjustments in routing tables that make dynamic routing possible. It has several advantages over static routing: superior scalability; the ability to adapt to failures and outages along network routes; and less manual configuration of the routing tables (routers learn from one another about their existence and available routes eliminating human errors). The disadvantages are complexity and additional network overhead from router communications, which consumes network bandwidth.</p>"},{"location":"networking-appendix/#network-configuration-files","title":"Network Configuration Files","text":"<p>In Ubuntu 24.04, networking is managed by Netplan. The configuration file to manage the network interfaces is <code>/etc/netplan/50-cloud-init.yaml</code> on systems configured with cloud-init and <code>/etc/netplan/00-installer-config.yaml</code> or <code>/etc/netplan/01-netcfg.yaml</code> on systems installed via ISO media. Here you can configure static, dynamic routing and DNS options.</p>"},{"location":"networking/","title":"7. Networking","text":"<p>Note</p> <p>Structured from the source material. Command validation is pending.</p> <p>Abstract</p> <p>Inspect interfaces, use ethtool, and diagnose network connectivity.</p>"},{"location":"networking/#71-basic-network-commands","title":"7.1 Basic Network Commands","text":"<p><code>ip</code> and <code>ethtool</code> inspect and modify basic network configuration. <code>ip</code> manages interfaces, addresses, routing, neighbours, and virtual devices. <code>ethtool</code> manages low-level device settings. Any user can usually inspect configuration, but only the superuser can modify it.</p> <p><code>ip</code> subcommands include <code>address</code>, <code>neighbour</code>, <code>link</code>, and <code>route</code>. Options include <code>-4</code> for IPv4, <code>-6</code> for IPv6, <code>-s</code> for statistics, <code>-d</code> for extra detail, and <code>-r</code> to resolve host names.</p> <pre><code># Show addresses for ens2\nip addr show dev ens2\n</code></pre> Expected result <p>Validation pending; no captured output is available.</p> <pre><code># Add an address to ens2\nsudo ip addr add 10.1.2.3/16 dev ens2\n</code></pre> Expected result <p>Validation pending; no captured output is available.</p> <pre><code># Show link statistics for ens2\nip -s link show dev ens2\n</code></pre> Expected result <p>Validation pending; no captured output is available.</p> <pre><code># Create VLAN 42 above ens2\nsudo ip link add dev vlan42 link ens2 type vlan id 42\n</code></pre> Expected result <p>Validation pending; no captured output is available.</p> <pre><code># Change the ens2 MAC address\nsudo ip link set dev ens2 address 02:42:43:44:45:56\n</code></pre> Expected result <p>Validation pending; no captured output is available.</p> <pre><code># List network interfaces\nip link show\n</code></pre> Expected result <p>Validation pending; no captured output is available.</p> <pre><code># Show ens2 addresses\nip addr show ens2\n</code></pre> Expected result <p>Validation pending; no captured output is available.</p> <pre><code># Add the lab address\nsudo ip addr add 10.1.2.3/15 dev ens2\n</code></pre> Expected result <p>Validation pending; no captured output is available.</p> <pre><code># Verify the additional address\nip addr show ens2\n</code></pre> Expected result <p>Validation pending; no captured output is available.</p> <pre><code># Show basic ens2 statistics\nip -s link show ens2\n</code></pre> Expected result <p>Validation pending; no captured output is available.</p> <pre><code># Show detailed ens2 statistics\nip -s -s link show ens2\n</code></pre> Expected result <p>Validation pending; no captured output is available.</p> <pre><code># Remove the lab address\nsudo ip addr delete 10.1.2.3/15 dev ens2\n</code></pre> Expected result <p>Validation pending; no captured output is available.</p> <pre><code># Show the routing table\nip route show\n</code></pre> Expected result <p>Validation pending; no captured output is available.</p> <pre><code># Show cached neighbour entries\nip neigh show\n</code></pre> Expected result <p>Validation pending; no captured output is available.</p> <pre><code># Show the route to 8.8.8.8\nip route get 8.8.8.8\n</code></pre> Expected result <p>Validation pending; no captured output is available.</p> <pre><code># Show ens2 link settings\nethtool ens2\n</code></pre> Expected result <p>Validation pending; no captured output is available.</p> <pre><code># Set ens2 link speed\nsudo ethtool -s ens2 speed 1000\n</code></pre> Expected result <p>Validation pending; no captured output is available.</p> <pre><code># Show ens2 ring sizes\nethtool -g ens2\n</code></pre> Expected result <p>Validation pending; no captured output is available.</p> <pre><code># Set the ens2 TX ring size\nsudo ethtool -G ens2 tx 1024\n</code></pre> Expected result <p>Validation pending; no captured output is available.</p> <pre><code># Show ens2 driver information\nethtool -i ens2\n</code></pre> Expected result <p>Validation pending; no captured output is available.</p> <pre><code># Show extended ens2 statistics\nethtool -S ens2\n</code></pre> Expected result <p>Validation pending; no captured output is available.</p> <pre><code># Show ens20 feature settings\nethtool -k ens20\n</code></pre> Expected result <p>Validation pending; no captured output is available.</p> <pre><code># Enable generic receive offload on ens2\nethtool -K ens2 gro on\n</code></pre> Expected result <p>Validation pending; no captured output is available.</p> <pre><code># Disable ens2\nsudo ifdown ens2\n</code></pre> Expected result <p>Validation pending; no captured output is available.</p> <pre><code># Enable ens2\nsudo ifup ens2\n</code></pre> Expected result <p>Validation pending; no captured output is available.</p> <pre><code># Install ethtool\nsudo apt install -y ethtool\n</code></pre> Expected result <p>Validation pending; no captured output is available.</p> <pre><code># Show ens2 hardware information\nethtool ens2\n</code></pre> Expected result <p>Validation pending; no captured output is available.</p> <pre><code># Show ens2 driver information\nethtool -i ens2\n</code></pre> Expected result <p>Validation pending; no captured output is available.</p> <pre><code># Show ens2 statistics\nethtool -S ens2\n</code></pre> Expected result <p>Validation pending; no captured output is available.</p> <pre><code># Show ens2 features\nethtool -k ens2\n</code></pre> Expected result <p>Validation pending; no captured output is available.</p> <pre><code># Show the permanent ens2 MAC address\nethtool -P ens2\n</code></pre> Expected result <p>Validation pending; no captured output is available.</p> <pre><code># Ping a host by address\nping 10.0.1.98\n</code></pre> Expected result <p>Validation pending; no captured output is available.</p> <pre><code># Ping a host by name\nping www.google.com\n</code></pre> Expected result <p>Validation pending; no captured output is available.</p> <pre><code># Ping canonical.com\nping canonical.com\n</code></pre> Expected result <p>Validation pending; no captured output is available.</p> <pre><code># Send three pings to ubuntu.com\nping -c 3 ubuntu.com\n</code></pre> Expected result <p>Validation pending; no captured output is available.</p> <pre><code># Send numeric-only pings to ubuntu.com\nping -c 3 -n ubuntu.com\n</code></pre> Expected result <p>Validation pending; no captured output is available.</p> <pre><code># Limit ping runtime to canonical.com\nping -w 2 canonical.com\n</code></pre> Expected result <p>Validation pending; no captured output is available.</p> <pre><code># Trace the route to google.com\ntraceroute google.com\n</code></pre> Expected result <p>Validation pending; no captured output is available.</p> <pre><code># Trace the route to ubuntu.com continuously\nmtr ubuntu.com\n</code></pre> Expected result <p>Validation pending; no captured output is available.</p> <pre><code># Listen with netcat on port 2389\nnc -l 2389\n</code></pre> Expected result <p>Validation pending; no captured output is available.</p> <pre><code># Connect to the local netcat listener\nnc localhost 2389\n</code></pre> Expected result <p>Validation pending; no captured output is available.</p> <pre><code># Query DNS for ubuntu.com\ndig ubuntu.com\n</code></pre> Expected result <p>Validation pending; no captured output is available.</p> <pre><code># Install traceroute\nsudo apt install -y traceroute\n</code></pre> Expected result <p>Validation pending; no captured output is available.</p> <pre><code># Send four pings to Google\nping -c 4 www.google.com\n</code></pre> Expected result <p>Validation pending; no captured output is available.</p> <pre><code># Trace the path to Google\ntraceroute www.google.com\n</code></pre> Expected result <p>Validation pending; no captured output is available.</p> <pre><code># Run an MTR trace to Google\nmtr www.google.com\n</code></pre> Expected result <p>Validation pending; no captured output is available.</p> <pre><code># Query Google DNS information\ndig www.google.com\n</code></pre> Expected result <p>Validation pending; no captured output is available.</p> <pre><code># Check ens2 packet-drop statistics\nethtool -S ens2\n</code></pre> Expected result <p>Validation pending; no captured output is available.</p>"},{"location":"prerequisites/","title":"Prerequisites","text":"<p>Abstract</p> <p>Each learner receives a dedicated outer Ubuntu student host that provides access to a ready LAB HOST.</p>"},{"location":"prerequisites/#lab-layout","title":"Lab layout","text":"<p>The outer <code>STUDENT HOST</code> is an Ubuntu 22.04 virtual machine. Its nested <code>LAB HOST</code> runs Ubuntu 24.04.2 and is reachable at <code>192.168.100.4</code>. Course staff prepare its network. Run all chapter exercises on the LAB HOST after you sign in.</p>"},{"location":"prerequisites/#access","title":"Access","text":"<p>You receive your individual outer-host address and credentials through the approved channel. Connect to the outer host as <code>ubuntu</code>, then sign in from there as <code>ubuntu@192.168.100.4</code>.</p>"},{"location":"prerequisites/#expected-initial-state","title":"Expected initial state","text":"<ul> <li>STUDENT HOST: Ubuntu 22.04.5, 4 vCPUs, about 16 GB of memory, and 40 GiB root storage.</li> <li>LAB HOST: Ubuntu 24.04.2, 2 vCPUs, about 4 GB of memory, and a <code>vda</code> system disk.</li> <li>LAB HOST: empty, disposable 10 GB <code>vdb</code> and <code>vdc</code> disks reserved for storage exercises.</li> </ul>"},{"location":"prerequisites/#check-your-environment","title":"Check your environment","text":"<p>Run the first two commands on the STUDENT HOST before signing in. Then sign in to the LAB HOST and run the remaining commands.</p> <pre><code># Show the STUDENT HOST operating-system release.\nlsb_release -d\n</code></pre> Expected result <p><code>Description:   Ubuntu 22.04.5 LTS</code></p> <pre><code># List the STUDENT HOST block devices.\nlsblk\n</code></pre> Expected result <pre><code>NAME    MAJ:MIN RM   SIZE RO TYPE MOUNTPOINTS\nloop0     7:0    0    74M  1 loop /snap/core22/2437\nloop1     7:1    0  66.8M  1 loop /snap/core24/1643\nloop2     7:2    0 352.3M  1 loop /snap/google-cloud-cli/489\nloop3     7:3    0 115.3M  1 loop /snap/lxd/40338\nloop4     7:4    0  50.1M  1 loop /snap/snapd/27710\nsda       8:0    0    40G  0 disk\n|-sda1    8:1    0  39.9G  0 part /\n|-sda14   8:14   0     4M  0 part\n`-sda15   8:15   0   106M  0 part /boot/efi\n</code></pre> <p>Device details can vary. Verify the root filesystem is on <code>sda1</code> and the disk is about 40 GB.</p> <pre><code># Show the LAB HOST operating-system release.\nlsb_release -d\n</code></pre> Expected result <p><code>Description:   Ubuntu 24.04.2 LTS</code></p> <pre><code># Show the number of LAB HOST processors available.\nnproc\n</code></pre> Expected result <p><code>2</code></p> <pre><code># Show LAB HOST memory use.\nfree -h\n</code></pre> Expected result <pre><code>               total        used        free      shared  buff/cache   available\nMem:           3.8Gi       407Mi       3.4Gi       984Ki       277Mi       3.4Gi\nSwap:             0B          0B          0B\n</code></pre> <p>Live memory use varies. Verify about 4 GB of memory is available.</p> <pre><code># List LAB HOST block devices.\nlsblk\n</code></pre> Expected result <pre><code>NAME    MAJ:MIN RM  SIZE RO TYPE MOUNTPOINTS\nvda     253:0    0  3.5G  0 disk\n|-vda1  253:1    0  2.5G  0 part /\n|-vda14 253:14   0    4M  0 part\n|-vda15 253:15   0  106M  0 part /boot/efi\n`-vda16 259:0    0  913M  0 part /boot\nvdb     253:16   0   10G  0 disk\nvdc     253:32   0   10G  0 disk\n</code></pre> <p>Device details can vary. Verify <code>vda</code> is the system disk and <code>vdb</code> and <code>vdc</code> are 10 GB disks without partitions.</p> <pre><code># Test LAB HOST name resolution and outbound connectivity.\nping -c 3 ubuntu.com\n</code></pre> Expected result <pre><code>PING ubuntu.com (185.125.190.20) 56(84) bytes of data.\n64 bytes from website-content-cache-1.ps5.canonical.com (185.125.190.20): icmp_seq=1 ttl=60 time=13.4 ms\n64 bytes from website-content-cache-1.ps5.canonical.com (185.125.190.20): icmp_seq=2 ttl=60 time=13.4 ms\n64 bytes from website-content-cache-1.ps5.canonical.com (185.125.190.20): icmp_seq=3 ttl=60 time=13.4 ms\n\n--- ubuntu.com ping statistics ---\n3 packets transmitted, 3 received, 0% packet loss, time 2003ms\nrtt min/avg/max/mdev = 13.421/13.426/13.434/0.005 ms\n</code></pre> <p>The resolved address and timings vary. Verify three replies and zero packet loss.</p>"},{"location":"prerequisites/#safety","title":"Safety","text":"<p>Warning</p> <p><code>/dev/vdb</code> and <code>/dev/vdc</code> are intentionally disposable lab disks. Later storage exercises partition, format, and overwrite them. Do not use them for personal data.</p>"},{"location":"process-management/","title":"8. Process management","text":"<p>Note</p> <p>This page is structured from the source material. Commands are pending validation.</p> <p>Abstract</p> <p>Define and manage processes, job control, priorities, and scheduled work with cron and at.</p>"},{"location":"process-management/#81-process-administration","title":"8.1 Process Administration","text":"<p>Starting with init, everything a computer does is a process, sometimes called a job or unit. The init system tracks each process with a process ID (PID). Each running process has a numerical directory at <code>/proc/[PID]</code> that contains process information.</p>"},{"location":"process-management/#listing-processes","title":"Listing processes","text":"<p><code>ps</code> lists processes. Without options, it lists the current user's processes and shows the PID, terminal (TTY), CPU time, and command.</p> <pre><code># List all processes with detailed information.\nps -ef\n</code></pre> Expected result <p>Validation pending; no captured output is available.</p> <p>To filter process output, pipe it through <code>grep</code>. To list processes by command name:</p> <pre><code># List processes named sshd.\nps -C sshd\n</code></pre> Expected result <p>Validation pending; no captured output is available.</p> <p><code>top</code> provides a continuously updated process display, ordered by CPU use by default.</p> <pre><code># Open the process monitor.\ntop -s\n</code></pre> Expected result <p>Validation pending; no captured output is available.</p>"},{"location":"process-management/#stopping-processes","title":"Stopping processes","text":"<p>Use <code>Ctrl+C</code> to stop a foreground process. For a background process, identify its PID and send a signal with <code>kill</code>.</p> <pre><code># Send a signal to a process by PID.\nkill option PID\n</code></pre> Expected result <p>Validation pending; no captured output is available.</p> <p>If no option is given, <code>kill</code> sends a termination signal for a clean shutdown. Common signals are <code>kill</code> or <code>kill -15</code> for <code>SIGTERM</code>, <code>kill -2</code> for <code>SIGINT</code>, and <code>kill -9</code> for <code>SIGKILL</code> as a last resort.</p>"},{"location":"process-management/#daemons","title":"Daemons","text":"<p>Daemons are continuously running background processes that are not connected to a terminal. Many have names ending in <code>d</code>, such as <code>cron</code>, which runs scheduled processes, and <code>sshd</code>, which accepts remote shell connections.</p>"},{"location":"process-management/#82-background-processes-and-priority","title":"8.2 Background Processes and Priority","text":"<p>A command entered in a terminal runs in the foreground and controls that terminal session. Append <code>&amp;</code> to run a job in the background.</p> <pre><code># Start a program in the background.\nprogramname &amp;\n</code></pre> Expected result <p>Validation pending; no captured output is available.</p> <p>Use <code>jobs</code> to list the current user's jobs. <code>-l</code> includes PIDs, <code>-r</code> shows only running jobs, and <code>-s</code> shows only stopped jobs.</p> <p>To move a foreground job to the background, press <code>Ctrl+Z</code> to suspend it, then run <code>bg</code>. Use <code>fg</code> to return the most recent background job to the foreground, or specify a job such as <code>fg %3</code>. End a background job with its PID or a job number such as <code>kill %2</code>.</p>"},{"location":"process-management/#process-priority","title":"Process priority","text":"<p>Processes start at priority 0. The range is <code>-20</code> (highest priority) through <code>19</code> (lowest priority). Use <code>nice</code> when starting a process with a non-default priority.</p> <pre><code># Start a command at the lowest priority.\nnice -n 19 commandname\n</code></pre> Expected result <p>Validation pending; no captured output is available.</p> <p>To change the priority of a running process, find its PID and use <code>renice</code>.</p> <pre><code># Give a process with PID 19365 a higher priority.\nrenice -10 -p 19365\n</code></pre> Expected result <p>Validation pending; no captured output is available.</p> <pre><code># Lower priority for a user's processes.\nrenice 15 -u greedyuser\n</code></pre> Expected result <p>Validation pending; no captured output is available.</p> <pre><code># Raise priority for a group's processes.\nrenice -18 -g website\n</code></pre> Expected result <p>Validation pending; no captured output is available.</p>"},{"location":"process-management/#83-scheduling-processes","title":"8.3 Scheduling Processes","text":"<p>Use <code>cron</code> to run recurring work and <code>at</code> for one-time work. This is useful for work such as overnight backups or CPU-intensive calculations.</p>"},{"location":"process-management/#scheduling-recurring-processes","title":"Scheduling recurring processes","text":"<p><code>cron</code> is a daemon started by init. It reads configuration and runs listed jobs at scheduled times. Use <code>crontab -e</code> to edit a user's crontab; it checks syntax before saving, and crontab files should not be edited directly.</p> <p>Each crontab line defines one job in this order:</p> <pre><code>Minute\nHour\nDay of the month\nMonth\nDay of the week\nCommand to run\n# m h dom mon dow command\n1 2 6 * * * rm -rf /home/codemonkey/sandbox/*\n</code></pre> <p>The example removes the contents of the sandbox directory at 6:12 AM every day. For system jobs, an administrator can place scripts or executables in <code>/etc/cron.daily</code>, <code>/etc/cron.hourly</code>, <code>/etc/cron.monthly</code>, or <code>/etc/cron.weekly</code>; <code>/etc/crontab</code> runs those directories at the corresponding frequency.</p>"},{"location":"process-management/#using-at","title":"Using at","text":"<p>Use <code>at</code> to schedule one-time work. You do not need to be logged in when it runs.</p> <pre><code># Schedule a one-time job for a relative or specific time.\nat now + 7 hours or at HH:MM\n</code></pre> Expected result <p>Validation pending; no captured output is available.</p> <p>Use <code>atq</code> to list scheduled jobs. Its output includes the job number, scheduled time, queue identifier, and user. Use <code>atrm</code> with the job number to remove a job.</p>"},{"location":"process-management/#831-process-management-lab","title":"8.3.1 Process Management Lab","text":"<p>Create <code>~/loop.sh</code> with nano, then enter this script:</p> <pre><code>#!/bin/bash\n\nwhile true\ndo\n    echo \"hello world\" &gt;&gt; hello.txt\n    sleep 1\ndone\n</code></pre> <pre><code># Create the loop script.\nnano ~/loop.sh\n</code></pre> Expected result <p>Validation pending; no captured output is available.</p> <pre><code># Make the loop script executable for its owner and group.\nchmod 750 ~/loop.sh\n</code></pre> Expected result <p>Validation pending; no captured output is available.</p> <pre><code># List processes owned by ubuntu.\nps -ef | grep ubuntu\n</code></pre> Expected result <p>Validation pending; no captured output is available.</p> <pre><code># List all running processes.\nps -ef\n</code></pre> Expected result <p>Validation pending; no captured output is available.</p> <pre><code># Monitor processes continuously.\ntop\n</code></pre> Expected result <p>Validation pending; no captured output is available.</p> <pre><code># Run the loop script in the foreground.\n./loop.sh\n</code></pre> Expected result <p>Validation pending; no captured output is available.</p> <p>Press <code>Ctrl+C</code> to stop the foreground <code>loop.sh</code> process.</p> <pre><code># Run the loop script in the background.\n./loop.sh &amp;\n</code></pre> Expected result <p>Validation pending; no captured output is available.</p> <pre><code># List jobs with their PIDs.\njobs -l\n</code></pre> Expected result <p>Validation pending; no captured output is available.</p> <pre><code># Stop the loop job using its PID.\nkill &lt;PID&gt;\n</code></pre> Expected result <p>Validation pending; no captured output is available.</p> <pre><code># Run the loop script in the background with nice value 15.\nnice -n 15 ./loop.sh &amp;\n</code></pre> Expected result <p>Validation pending; no captured output is available.</p> <pre><code># Find the SSH daemon PID.\nps aux | grep sshd\n</code></pre> Expected result <p>Validation pending; no captured output is available.</p> <pre><code># Set the SSH daemon priority to -5.\nsudo renice -5 -p &lt;PID&gt;\n</code></pre> Expected result <p>Validation pending; no captured output is available.</p> <pre><code># Check the current nice value for the process.\nps -o ni &lt;PID&gt;\n</code></pre> Expected result <p>Validation pending; no captured output is available.</p> <p>Danger</p> <p>The next command forcefully terminates the process identified by the PID. Confirm the PID before running it.</p> <pre><code># Forcefully terminate the specified process.\nsudo kill -9 &lt;PID&gt;\n</code></pre> Expected result <p>Validation pending; no captured output is available.</p> <pre><code># Edit the current user's crontab.\ncrontab -e\n</code></pre> Expected result <p>Validation pending; no captured output is available.</p> <p>Add this line to schedule creation of <code>/tmp/mytest.txt</code> every five minutes:</p> <pre><code>*/5 * * * * touch /tmp/mytest.txt\n</code></pre> <pre><code># Install the at scheduler.\nsudo apt install at -y\n</code></pre> Expected result <p>Validation pending; no captured output is available.</p> <pre><code># Open an at prompt for 2:00 PM.\nat 14:00\n</code></pre> Expected result <p>Validation pending; no captured output is available.</p> <p>At the prompt, enter <code>rm /tmp/*yaml</code>, then press <code>Ctrl+D</code> to submit the job.</p> <pre><code># List scheduled at jobs.\natq\n</code></pre> Expected result <p>Validation pending; no captured output is available.</p> <pre><code># Install htop.\nsudo apt install -y htop\n</code></pre> Expected result <p>Validation pending; no captured output is available.</p> <pre><code># Monitor processes with htop.\nhtop\n</code></pre> Expected result <p>Validation pending; no captured output is available.</p> <p>End of the lab. Do not continue to the next topic.</p>"},{"location":"software-management/","title":"10. Software Management","text":"<p>Note</p> <p>This page is structured from the source material. Commands are pending validation.</p> <p>Abstract</p> <p>Manage Debian packages with dpkg and apt, and manage software with snaps.</p>"},{"location":"software-management/#101-debian-package-management","title":"10.1 Debian Package Management","text":"<p>Packages simplify software installation and removal, dependency management, conflict prevention, and inventory of installed and configured software.</p> <p>Ubuntu archives provide two package forms: <code>deb</code> binary archives containing executables, configuration, copyright information, and documentation; and <code>dsc</code> packages containing original source code and details of changes to it.</p> <p><code>dpkg</code> installs, builds, removes, and manages Debian package files. It reads package instructions and packs or unpacks files as needed. Package management often requires administrator privileges.</p> <p>Common <code>dpkg</code> commands are <code>dpkg -i packagename.deb</code> to install, <code>dpkg -r packagename.deb</code> to remove while retaining configuration, <code>dpkg -P packagename.deb</code> to purge including configuration, <code>dpkg -l</code> to list installed packages, <code>dpkg -l *name-pattern*</code> to list matches, <code>dpkg -L package-name</code> to list installed files, and <code>dpkg -S filename-search-pattern</code> to find an installed package owning a file.</p>"},{"location":"software-management/#102-advanced-package-tool-apt","title":"10.2 Advanced Package Tool (Apt)","text":"<p><code>apt</code> adds automatic dependency management and easier upgrades to Debian package management. Repositories are listed in <code>/etc/apt/sources.list</code> and files in <code>/etc/apt/sources.list.d/</code>.</p> <p>The main Ubuntu repositories are <code>main</code> for officially supported free software, <code>restricted</code> for officially supported software without a completely free license, <code>universe</code> for community-maintained software, and <code>multiverse</code> for community-maintained software without a completely free license.</p> <p>Personal Package Archives (PPAs) on Launchpad can provide more frequent software updates than official repositories. Use apt commands rather than editing source lists directly.</p> <pre><code># Add a repository.\napt-add-repository \"https://pathtorepo.net main\"\n</code></pre> Expected result <p>Validation pending; no captured output is available.</p> <pre><code># Add a Personal Package Archive.\napt-add-repository ppa://shinysoft/devel\n</code></pre> Expected result <p>Validation pending; no captured output is available.</p> <pre><code># Remove a Personal Package Archive.\napt-add-repository --remove ppa://shinysoft/devel\n</code></pre> Expected result <p>Validation pending; no captured output is available.</p> <p>Refresh the package list after adding or removing repositories, and before installing or upgrading packages.</p> <pre><code># Refresh available package metadata.\napt update\n</code></pre> Expected result <p>Validation pending; no captured output is available.</p> <pre><code># Install a package and its dependencies.\napt install packagename\n</code></pre> Expected result <p>Validation pending; no captured output is available.</p> <pre><code># Remove a package while retaining configuration files.\napt remove packagename\n</code></pre> Expected result <p>Validation pending; no captured output is available.</p> <pre><code># Remove unneeded dependencies.\napt autoremove\n</code></pre> Expected result <p>Validation pending; no captured output is available.</p> <pre><code># Purge a package and its configuration files.\napt purge packagename\n</code></pre> Expected result <p>Validation pending; no captured output is available.</p> <p><code>apt upgrade</code> installs newer versions of installed packages. <code>apt dist-upgrade</code> also handles changing dependencies, prioritizing important packages if needed. <code>apt full-upgrade</code> may remove installed packages when required to upgrade the system as a whole.</p> <p>For a menu-driven package interface, use <code>aptitude</code>. It can also be used from the command line and automatically removes dependencies when removing a package; with apt, use <code>apt autoremove</code>.</p>"},{"location":"software-management/#103-snappy-package-management","title":"10.3 Snappy Package Management","text":"<p>Snap is an additional Ubuntu package format, not a replacement for deb packages. It helps developers package applications with their dependencies and deliver transactional updates. A snap includes its software and dependencies, provides app isolation, supports an app-store model, and avoids dependency changes in shared libraries.</p> <pre><code># Install a snap package.\nsnap install packagename\n</code></pre> Expected result <p>Validation pending; no captured output is available.</p> <pre><code># Find available snaps.\nsnap find\n</code></pre> Expected result <p>Validation pending; no captured output is available.</p> <pre><code># Search available snaps.\nsnap find searchterm\n</code></pre> Expected result <p>Validation pending; no captured output is available.</p> <pre><code># List installed snaps.\nsnap list\n</code></pre> Expected result <p>Validation pending; no captured output is available.</p> <pre><code># Refresh a snap to its latest release.\nsnap refresh packagename\n</code></pre> Expected result <p>Validation pending; no captured output is available.</p> <pre><code># Remove an installed snap.\nsnap remove packagename\n</code></pre> Expected result <p>Validation pending; no captured output is available.</p>"},{"location":"software-management/#holding-snap-refreshes","title":"Holding snap refreshes","text":"<p><code>snap refresh --hold</code> postpones updates for individual snaps or all snaps, indefinitely or for a duration. Units may be seconds (<code>s</code>), minutes (<code>m</code>), hours (<code>h</code>), or a combination; <code>forever</code> is valid for an indefinite hold.</p> <pre><code># Hold refreshes for named snaps.\nsnap refresh --hold=&lt;duration&gt; &lt;snap1&gt; &lt;snap2&gt;...\n</code></pre> Expected result <p>Validation pending; no captured output is available.</p> <pre><code># Hold Firefox refreshes for 24 hours.\nsnap refresh --hold=24h firefox\n</code></pre> Expected result <p>Validation pending; no captured output is available.</p> <p>If no duration is specified, the hold defaults to <code>forever</code>. If no snaps are named, the hold applies to all installed snaps.</p> <pre><code># Hold automatic refreshes for all snaps for 24 hours.\nsnap refresh --hold=24h\n</code></pre> Expected result <p>Validation pending; no captured output is available.</p> <pre><code># List installed snaps and their hold status.\nsnap list\n</code></pre> Expected result <p>Validation pending; no captured output is available.</p> <p>When snaps are named, the hold blocks their auto-refreshes and general <code>snap refresh</code> requests, but not a targeted refresh. This allows the rest of the system to refresh while a known-problematic snap is held. When no snaps are named, the hold applies to all installed snaps but blocks only auto-refreshes, not general or targeted refresh requests.</p> <pre><code># Remove a hold from Firefox.\nsnap refresh --unhold firefox\n</code></pre> Expected result <p>Validation pending; no captured output is available.</p> <pre><code># Remove the automatic refresh hold from all snaps.\nsnap refresh --unhold\n</code></pre> Expected result <p>Validation pending; no captured output is available.</p>"},{"location":"software-management/#1031-software-management-lab","title":"10.3.1 Software Management Lab","text":"<pre><code># List active apt source entries.\ncat /etc/apt/sources.list | grep -v \"^#\" | grep -v \"^$\"\n</code></pre> Expected result <p>Validation pending; no captured output is available.</p> <pre><code># Show available MAAS package versions.\napt-cache policy maas\n</code></pre> Expected result <p>Validation pending; no captured output is available.</p> <pre><code># Install repository management support.\nsudo apt install -y software-properties-common\n</code></pre> Expected result <p>Validation pending; no captured output is available.</p> <pre><code># Add the Audacious PPA.\nsudo apt-add-repository ppa:ubuntuhandbook1/apps\n</code></pre> Expected result <p>Validation pending; no captured output is available.</p> <pre><code># Refresh the package list.\nsudo apt update\n</code></pre> Expected result <p>Validation pending; no captured output is available.</p> <pre><code># Show available Audacious package versions.\napt-cache policy audacious\n</code></pre> Expected result <p>Validation pending; no captured output is available.</p> <pre><code># Remove the Audacious PPA.\nsudo apt-add-repository --remove ppa:ubuntuhandbook1/apps\n</code></pre> Expected result <p>Validation pending; no captured output is available.</p> <pre><code># Refresh package metadata after removing the PPA.\nsudo apt update\n</code></pre> Expected result <p>Validation pending; no captured output is available.</p> <pre><code># Install nullmailer using default configuration values.\nsudo apt install -y nullmailer\n</code></pre> Expected result <p>Validation pending; no captured output is available.</p> <pre><code># Show the nullmailer package status.\ndpkg -l nullmailer\n</code></pre> Expected result <p>Validation pending; no captured output is available.</p> <p>The first status character is the desired state: <code>u</code> unknown, <code>i</code> install, <code>r</code> remove, <code>p</code> purge, or <code>h</code> hold. The second is the current state: <code>n</code> not installed, <code>i</code> installed, <code>c</code> configuration files present, <code>u</code> unpacked, <code>f</code> failed configuration removal, <code>h</code> half-installed, <code>W</code> trigger awaiting, or <code>t</code> trigger pending.</p> <pre><code># Remove nullmailer without purging its configuration.\nsudo apt remove -y nullmailer\n</code></pre> Expected result <p>Validation pending; no captured output is available.</p> <pre><code># Show the removed nullmailer package status.\ndpkg -l nullmailer\n</code></pre> Expected result <p>Validation pending; no captured output is available.</p> <pre><code># Purge nullmailer and its configuration.\nsudo apt purge -y nullmailer\n</code></pre> Expected result <p>Validation pending; no captured output is available.</p> <pre><code># Confirm nullmailer no longer has a package entry.\ndpkg -l nullmailer\n</code></pre> Expected result <p>Validation pending; no captured output is available.</p> <pre><code># Search available packages for lynx.\napt-cache search lynx\n</code></pre> Expected result <p>Validation pending; no captured output is available.</p> <pre><code># Show information about the lynx package.\napt-cache show lynx\n</code></pre> Expected result <p>Validation pending; no captured output is available.</p> <pre><code># Install snapd.\nsudo apt install snapd -y\n</code></pre> Expected result <p>Validation pending; no captured output is available.</p> <pre><code># List installed snap applications.\nsnap list\n</code></pre> Expected result <p>Validation pending; no captured output is available.</p> <pre><code># Find the hello-world snap.\nsnap find hello-world\n</code></pre> Expected result <p>Validation pending; no captured output is available.</p> <pre><code># Install the hello-world snap.\nsudo snap install hello-world\n</code></pre> Expected result <p>Validation pending; no captured output is available.</p> <pre><code># Run the hello-world snap.\nhello-world\n</code></pre> Expected result <p>Validation pending; no captured output is available.</p> <pre><code># Refresh hello-world to the latest version.\nsudo snap refresh hello-world\n</code></pre> Expected result <p>Validation pending; no captured output is available.</p> <pre><code># Hold hello-world refreshes for 24 hours.\nsudo snap refresh --hold=24h hello-world\n</code></pre> Expected result <p>Validation pending; no captured output is available.</p> <pre><code># Remove the hello-world refresh hold.\nsudo snap refresh --unhold hello-world\n</code></pre> Expected result <p>Validation pending; no captured output is available.</p> <pre><code># Remove the hello-world snap.\nsudo snap remove hello-world\n</code></pre> Expected result <p>Validation pending; no captured output is available.</p> <p>End of the lab. Do not continue to the next topic.</p>"},{"location":"storage/","title":"6. Storage","text":"<p>Note</p> <p>Structured from the source material. Command validation is pending.</p> <p>Abstract</p> <p>Partition block devices, create and mount ext4 filesystems, and use LVM.</p> <p>Danger</p> <p>Partitioning, formatting, and LVM commands can destroy data. The source lab reserves the first block device for the host operating system; do not use it for lab activity.</p>"},{"location":"storage/#61-partitioning","title":"6.1 Partitioning","text":"<p>A partition is space on a block device allocated to a filesystem. A device can have one partition spanning the device or multiple partitions. The partition table describes the layout and is read before other device data. Linux uses MBR and GPT partition-table schemes.</p> <p>MBR supports older hardware, up to four primary partitions, and disks or partitions no larger than 2 TiB. It requires legacy mode with grub-pc and stores GRUB in the MBR. GPT supports more primary partitions, larger disks and partitions, and a redundant partition-information copy for recovery. <code>parted</code> can create, destroy, resize, move, and copy partitions.</p>"},{"location":"storage/#611-partitioning-lab","title":"6.1.1 Partitioning Lab","text":"<pre><code># List block devices\nsudo lsblk\n</code></pre> Expected result <p>Validation pending; no captured output is available.</p> <pre><code># Inspect partition tables\nsudo fdisk -l\n</code></pre> Expected result <p>Validation pending; no captured output is available.</p> <pre><code># Create a GPT table on vdb\nsudo parted /dev/vdb mklabel gpt\n</code></pre> Expected result <p>Validation pending; no captured output is available.</p> <pre><code># Verify the vdb partition table\nsudo fdisk -l /dev/vdb\n</code></pre> Expected result <p>Validation pending; no captured output is available.</p> <pre><code># Create a partition spanning vdb\nsudo parted /dev/vdb mkpart primary 1 100%\n</code></pre> Expected result <p>Validation pending; no captured output is available.</p> <pre><code># Show vdb partition details\nsudo parted /dev/vdb print\n</code></pre> Expected result <p>Validation pending; no captured output is available.</p> <pre><code># Create an MBR table on vdc\nsudo parted /dev/vdc mklabel msdos\n</code></pre> Expected result <p>Validation pending; no captured output is available.</p> <pre><code># Create a partition spanning vdc\nsudo parted /dev/vdc mkpart primary 1 100%\n</code></pre> Expected result <p>Validation pending; no captured output is available.</p> <pre><code># Show vdc partition details\nsudo parted /dev/vdc print\n</code></pre> Expected result <p>Validation pending; no captured output is available.</p> <pre><code># Remove the vdc partition\nsudo parted /dev/vdc rm 1\n</code></pre> Expected result <p>Validation pending; no captured output is available.</p> <pre><code># Create an ext4 filesystem on vdb1\nsudo mkfs.ext4 /dev/vdb1\n</code></pre> Expected result <p>Validation pending; no captured output is available.</p> <pre><code># Enable the ext4 ACL mount option\nsudo tune2fs -o acl /dev/vdb1\n</code></pre> Expected result <p>Validation pending; no captured output is available.</p> <pre><code># Mount vdb1 at the mount point\nsudo mount /dev/vdb1 /mnt/mymount\n</code></pre> Expected result <p>Validation pending; no captured output is available.</p> <pre><code># Create the mount point\nsudo mkdir /mnt/mymount\n</code></pre> Expected result <p>Validation pending; no captured output is available.</p> <pre><code># Define an fstab entry by device\n/dev/vdb2 /boot ext4 defaults 0 2\n</code></pre> Expected result <p>Validation pending; no captured output is available.</p> <pre><code># Define an fstab entry by UUID\nUUID=1ea4363f-f9ab-45d8-af48-067b05d202b2 /boot ext4 defaults 0 2\n</code></pre> Expected result <p>Validation pending; no captured output is available.</p> <pre><code># Define a user-mountable fstab entry by label\nLABEL=mystuff /mystuff ext4 noauto,user 0 0\n</code></pre> Expected result <p>Validation pending; no captured output is available.</p> <pre><code># List block devices\nsudo lsblk\n</code></pre> Expected result <p>Validation pending; no captured output is available.</p> <pre><code># Format vdb1 as ext4\nsudo mkfs.ext4 /dev/vdb1\n</code></pre> Expected result <p>Validation pending; no captured output is available.</p> <pre><code># Create the ext4 mount point\nsudo mkdir /mnt/myext4fs\n</code></pre> Expected result <p>Validation pending; no captured output is available.</p> <pre><code># Mount vdb1\nsudo mount /dev/vdb1 /mnt/myext4fs\n</code></pre> Expected result <p>Validation pending; no captured output is available.</p> <pre><code># Verify the vdb mount\nmount | grep vdb\n</code></pre> Expected result <p>Validation pending; no captured output is available.</p> <pre><code># Display available filesystem space\ndf -h\n</code></pre> Expected result <p>Validation pending; no captured output is available.</p> <pre><code># Edit fstab\nsudo nano /etc/fstab\n</code></pre> Expected result <p>Validation pending; no captured output is available.</p> <pre><code># Add the vdb1 fstab entry\n/dev/vdb1   /mnt/myext4fs   ext4    defaults    0 0\n</code></pre> Expected result <p>Validation pending; no captured output is available.</p> <pre><code># Unmount vdb1\nsudo umount /dev/vdb1\n</code></pre> Expected result <p>Validation pending; no captured output is available.</p> <pre><code># Mount all fstab filesystems\nsudo mount -a\n</code></pre> Expected result <p>Validation pending; no captured output is available.</p> <pre><code># Confirm the fstab mount\nmount | grep vdb\n</code></pre> Expected result <p>Validation pending; no captured output is available.</p> <pre><code># Unmount the filesystem\nsudo umount /dev/vdb1\n</code></pre> Expected result <p>Validation pending; no captured output is available.</p> <pre><code># Partition vdb for LVM\nfdisk /dev/vdb\n</code></pre> Expected result <p>Validation pending; no captured output is available.</p> <pre><code># Create physical volumes\npvcreate /dev/vda3 /dev/vda4\n</code></pre> Expected result <p>Validation pending; no captured output is available.</p> <pre><code># Create a volume group\nvgcreate ubuntu-vg /dev/vda3 /dev/vda4\n</code></pre> Expected result <p>Validation pending; no captured output is available.</p> <pre><code># Create a logical volume\nlvcreate -L 100G -n UB2 ubuntu-vg\n</code></pre> Expected result <p>Validation pending; no captured output is available.</p> <pre><code># Find 10 GB block devices\nsudo lsblk -a -p -o name,size | grep 10G\n</code></pre> Expected result <p>Validation pending; no captured output is available.</p> <pre><code># Overwrite vdb with zeros\nsudo dd if=/dev/zero of=/dev/vdb bs=4096\n</code></pre> Expected result <p>Validation pending; no captured output is available.</p> <pre><code># Overwrite vdc with zeros\nsudo dd if=/dev/zero of=/dev/vdc bs=4096\n</code></pre> Expected result <p>Validation pending; no captured output is available.</p> <pre><code># Create a GPT label on vdb\nsudo parted /dev/vdb mklabel gpt\n</code></pre> Expected result <p>Validation pending; no captured output is available.</p> <pre><code># Create a GPT label on vdc\nsudo parted /dev/vdc mklabel gpt\n</code></pre> Expected result <p>Validation pending; no captured output is available.</p> <pre><code># Create a full vdb partition\nsudo parted /dev/vdb mkpart primary 1 100%\n</code></pre> Expected result <p>Validation pending; no captured output is available.</p> <pre><code># Create a full vdc partition\nsudo parted /dev/vdc mkpart primary 1 100%\n</code></pre> Expected result <p>Validation pending; no captured output is available.</p> <pre><code># Install LVM tools\nsudo apt install -y lvm2\n</code></pre> Expected result <p>Validation pending; no captured output is available.</p> <pre><code># Scan existing volume groups\nsudo vgscan\n</code></pre> Expected result <p>Validation pending; no captured output is available.</p> <pre><code># Create LVM physical volumes\nsudo pvcreate /dev/vdb1 /dev/vdc1\n</code></pre> Expected result <p>Validation pending; no captured output is available.</p> <pre><code># List physical volumes\nsudo pvs\n</code></pre> Expected result <p>Validation pending; no captured output is available.</p> <pre><code># Create the ubuntu-vg volume group\nsudo vgcreate ubuntu-vg /dev/vdb1 /dev/vdc1\n</code></pre> Expected result <p>Validation pending; no captured output is available.</p> <pre><code># List volume groups\nsudo vgs\n</code></pre> Expected result <p>Validation pending; no captured output is available.</p> <pre><code># Create the lvmdata logical volume\nsudo lvcreate -l 100%VG -n lvmdata ubuntu-vg\n</code></pre> Expected result <p>Validation pending; no captured output is available.</p> <pre><code># List logical volumes\nsudo lvs\n</code></pre> Expected result <p>Validation pending; no captured output is available.</p> <pre><code># Display logical volume details\nsudo lvdisplay\n</code></pre> Expected result <p>Validation pending; no captured output is available.</p> <pre><code># Format the logical volume as ext4\nsudo mkfs.ext4 /dev/ubuntu-vg/lvmdata\n</code></pre> Expected result <p>Validation pending; no captured output is available.</p> <pre><code># Create the LVM mount point\nsudo mkdir /lvmdata\n</code></pre> Expected result <p>Validation pending; no captured output is available.</p> <pre><code># Mount the logical volume\nsudo mount /dev/ubuntu-vg/lvmdata /lvmdata\n</code></pre> Expected result <p>Validation pending; no captured output is available.</p> <pre><code># Show mounted logical-volume space\ndf -h /lvmdata\n</code></pre> Expected result <p>Validation pending; no captured output is available.</p>"},{"location":"what-is-ubuntu/","title":"1. What is Ubuntu","text":"<p>Goals</p> <p>Differentiate Ubuntu flavours, explain the release cycle and naming convention, and describe the LTS maintenance cycle.</p> <p>Note</p> <p>Commands in this chapter were validated on the LAB HOST on 2026-08-24.</p> <p>Connect to the <code>STUDENT HOST</code> using the public IP and credentials provided for the lab. Then connect to the <code>LAB HOST</code> to run the commands in subsequent chapters.</p> <pre><code># Connect to the lab host.\nssh ubuntu@192.168.100.4\n</code></pre> Expected result <p>Welcome to Ubuntu 24.04.2 LTS (GNU/Linux 6.8.0-53-generic x86_64)</p> <p>Welcome text and the shell prompt can differ between lab sessions. Authentication prompts are not shown.</p>"},{"location":"what-is-ubuntu/#11-what-is-linux","title":"1.1 What Is Linux?","text":"<p>Linux began as a clone of MINIX, a Unix-like system intended for academic use. It evolved into the Linux kernel, created and released by Linus Torvalds in 1991 as a free and later open-source alternative to MINIX and other UNIX-like systems with restrictions on modification and redistribution.</p> <p>Linux is also commonly used to mean an operating system: a set of programs, tools, and services bundled with the Linux kernel to provide the components of an operating system.</p> <p>Linux is the largest and most widespread open-source software project. The Linux Foundation fosters kernel development. Linux runs on systems from embedded devices to supercomputers. Examples from the source material include:</p> <ul> <li>95% of domains use Linux as the operating system.</li> <li>80% of smartphones run Android, which is based on the Linux kernel.</li> <li>98% of the 500 fastest supercomputers run Linux.</li> <li>75% of cloud-enabled enterprises use Linux as the primary cloud platform.</li> <li>NYSE, NASDAQ, the London Exchange, and the Tokyo Stock Exchange run on Linux.</li> <li>Most consumer electronic devices use Linux because of its small footprint.</li> <li>Amazon, Ebay, Paypal, Walmart, and others use Linux.</li> </ul> <p>Linux is also widely used for the Internet of Things (IoT), cloud computing, and big data.</p>"},{"location":"what-is-ubuntu/#12-what-is-the-kernel","title":"1.2 What Is the Kernel?","text":"<p>The Linux kernel is at the core of Ubuntu. It manages hardware resources including I/O devices, memory, and CPU. It is one of the first programs loaded during boot and provides a hardware environment in which user-space and application programs can run across different platforms without needing to know the underlying system in detail.</p> <p>More information is available at https://kernel.org/.</p> <p>The kernel controls CPU, memory, inter-process communication, device drivers, filesystem management, and system calls. Because it manages hardware and multitasking, it can provide processes with information from memory or other processes and enable communication between processes.</p> <p>The Linux kernel is monolithic: device drivers, filesystems, and application inter-process communication operate in kernel space. The kernel defines the high-level virtual interface over hardware. System calls provide OS services such as process management, concurrency, and memory management. Device drivers can be added as modules.</p> <p>Linux supports true preemptive multitasking, virtual memory, shared libraries, demand loading, shared copy-on-write executables, memory management, the Internet protocol suite, and threading.</p> <p>The kernel is written in C and assembly language by contributors worldwide. Development discussion is available through the Linux Kernel Mailing List (LKML).</p> <p>The kernel runs in kernel mode; user processes run in user mode. Kernel-mode code has unrestricted processor and main-memory access. Most software runs in user space and interfaces with the kernel to perform tasks.</p> <p>Included modules and kernel configuration determine a kernel's functionality for its hardware and expected workloads. Kernel modules are binary programs that extend hardware control or add capabilities such as high-performance networking or non-standard graphics. The default <code>GA</code> kernel shipped with Canonical Ubuntu LTS and Hardware Enablement (<code>HWE</code>) releases is tuned for stable, reliable, secure, high-performance operation across varied hardware and workloads.</p> <p></p>"},{"location":"what-is-ubuntu/#13-what-is-ubuntu","title":"1.3 What Is Ubuntu?","text":"<p>Ubuntu is a Linux distribution based on Debian that runs on platforms from IoT to the cloud.</p> <p>Ubuntu release names use a two-digit year and two-digit release month. For example, Ubuntu 24.04 was released in April 2024 and Ubuntu 23.10 was released in October 2023. Releases also have an <code>Adjective Animal</code> code name: Ubuntu 24.04 is <code>Noble Numbat</code>, and Ubuntu 24.10 is <code>Oracular Oriole</code>.</p>"},{"location":"what-is-ubuntu/#14-the-release-cycle","title":"1.4 The Release Cycle","text":"<p>LTS (<code>Long Term Support</code>) releases are published every two years in April. They are Ubuntu's enterprise-grade releases and are the most widely used; the source estimates that 95% of Ubuntu installations use LTS releases.</p> <p>Canonical publishes an interim release every six months between LTS versions. Ubuntu 24.10 is an example. Interim releases are production-quality and supported for nine months, allowing time to update, but they do not have the long-term commitment of LTS releases.</p> <p></p> <p>Interim releases introduce new capabilities from Canonical and upstream open-source projects and act as a proving ground. Developers may use them for newer compilers, kernels, and libraries, including in rapid DevOps processes such as CI/CD where an artifact may have a shorter lifespan than the interim support period. Interim releases receive full security maintenance for <code>main</code> during their lifespan.</p> <p>For more information, visit http://www.ubuntu.com.</p>"},{"location":"what-is-ubuntu/#15-ubuntu-lab","title":"1.5 Ubuntu Lab","text":"<ol> <li>Check the Ubuntu distribution version.</li> </ol> <pre><code># Display operating system release metadata.\ncat /etc/os-release\n</code></pre> Expected result <p>PRETTY_NAME=\"Ubuntu 24.04.2 LTS\" NAME=\"Ubuntu\" VERSION_ID=\"24.04\" VERSION=\"24.04.2 LTS (Noble Numbat)\" VERSION_CODENAME=noble ID=ubuntu ID_LIKE=debian HOME_URL=\"https://www.ubuntu.com/\" SUPPORT_URL=\"https://help.ubuntu.com/\" BUG_REPORT_URL=\"https://bugs.launchpad.net/ubuntu/\" PRIVACY_POLICY_URL=\"https://www.ubuntu.com/legal/terms-and-policies/privacy-policy\" UBUNTU_CODENAME=noble LOGO=ubuntu-logo</p> <p>Another way to check the Ubuntu distribution version is:</p> <pre><code># Display legacy Ubuntu release metadata when the file is present.\ncat /etc/lsb-release\n</code></pre> Expected result <p>DISTRIB_ID=Ubuntu DISTRIB_RELEASE=24.04 DISTRIB_CODENAME=noble DISTRIB_DESCRIPTION=\"Ubuntu 24.04.2 LTS\"</p> <ol> <li>Check the kernel version.</li> </ol> <pre><code># Display kernel and system information.\nuname -a\n</code></pre> Expected result <p>Linux ubuntu 6.8.0-53-generic #55-Ubuntu SMP PREEMPT_DYNAMIC Fri Jan 17 15:37:52 UTC 2025 x86_64 x86_64 x86_64 GNU/Linux</p> <p>The kernel version, build timestamp, and system architecture can differ between lab images.</p> <p>Another way to check the kernel version is:</p> <pre><code># Display Ubuntu's kernel version signature.\ncat /proc/version_signature\n</code></pre> Expected result <p>Ubuntu 6.8.0-53.55-generic 6.8.12</p> <p>The package and upstream kernel versions can differ between lab images.</p> <p>End of the lab. Do not continue with the next topic.</p>"},{"location":"ci-test-repo/build_and_deploy/","title":"Build and Deploy Instructions","text":""},{"location":"ci-test-repo/build_and_deploy/#prepare-local-repo","title":"Prepare local repo","text":""},{"location":"ci-test-repo/build_and_deploy/#clone-conductor-repository","title":"Clone conductor repository","text":"<pre><code>git clone git@github.com:virtomat/ci-test-repo.git virt-ci-test-repo\n</code></pre> Initial directory structure should look like this: (expand to see) <pre><code>radu@ThinkPad-L380$ tree -L 2\n.\n\u251c\u2500\u2500 docs\n\u2502    \u251c\u2500\u2500 README.md\n\u2502    \u2514\u2500\u2500 test.md\n\u251c\u2500\u2500 helm\n\u2502    \u251c\u2500\u2500 Chart.yaml\n\u2502    \u251c\u2500\u2500 templates\n\u2502    \u2502    \u251c\u2500\u2500 deployment.yaml\n\u2502    \u2502    \u251c\u2500\u2500 ingress.yaml\n\u2502    \u2502    \u2514\u2500\u2500 service.yaml\n\u2502    \u2514\u2500\u2500 values.yaml\n\u251c\u2500\u2500 app.py\n\u251c\u2500\u2500 Dockerfile\n\u251c\u2500\u2500 logger.py\n\u251c\u2500\u2500 README.md\n\u2514\u2500\u2500 requirements.txt\n</code></pre>"},{"location":"ci-test-repo/build_and_deploy/#docker-image","title":"Docker Image","text":"<p>You can build the image with (local usage): <pre><code>docker build . -t testapp\n</code></pre></p> <p>Note</p> <p>As this repo is integrated on the CI pipeline, there is no need to push the image Building the image locally is for testing purposes when (for some reason) the build fails.</p>"},{"location":"ci-test-repo/build_and_deploy/#helm-chart","title":"Helm Chart","text":""},{"location":"ci-test-repo/build_and_deploy/#prerequisites","title":"Prerequisites","text":"<p>Create regcred docker registry secret. This has to have Read permission over virtomat project (to pull cixpress images) and Read/Write permissions over target project. <pre><code>kubectl -n cixpress create secret docker-registry regcred \\\n           --docker-server=cr.virtomat.io \\\n           --docker-username='robot-virtomat+cixpress' \\\n           --docker-password='07NESZAGTyubvOW2aIOTHMBVlxuTGZ3o'\n</code></pre></p>"},{"location":"ci-test-repo/build_and_deploy/#installation-dev-cluster","title":"Installation (Dev Cluster)","text":"<p>From source code repository(for reference only): <pre><code>helm install testapp ./helm\n</code></pre> From published helm chart: <pre><code>helm install testapp oci://cr.virtomat.io/virtomat/cixpress/helm/testapp\n</code></pre></p> <p>Note</p> <p>As this chart is integrated on the CD pipeline, there is no need to install it manually. CD is managed with ArgoCD</p>"},{"location":"ci-test-repo/build_and_deploy/#installation-local-dev-cluster","title":"Installation (Local Dev Cluster)","text":"<p><pre><code>helm -n cixpress install conductor ./helm \\\n          --set logging=DEBUG \\\n          --set ingress.enableTLS=false \\\n          --set persistence.mount_dev_dir=true \\\n          --set dev_dir_path=/home/radu/Dev/virt-ci-test-repo\n</code></pre> <pre><code>helm -n cixpress install conductor oci://cr.virtomat.io/virtomat/cixpress/helm/conductor \\\n          --set logging=DEBUG \\\n          --set ingress.enableTLS=false \\\n          --set persistence.mount_dev_dir=true \\\n          --set dev_dir_path=/home/radu/Dev/virt-ci-test-repo\n</code></pre></p> Mounting local workdir inside the runtime container <p>Mounting local workdir inside the runtime container allows live modifications (in your IDE of choice) and reloads of the application runtime without any workarounds. This requires a working local microk8s cluster</p> Proxy-forward webhook calls from github <p>Proxy-forward webhook calls from github to local environment (https://www.ultrahook.com/) <pre><code>ultrahook conductor http://ci.dev.virtomat.io/conductor/start-build-pipeline\n</code></pre></p>"},{"location":"ci-test-repo/build_and_deploy/#verify-the-installation","title":"Verify the installation","text":"<p>Check that the pods are fine (running and ready): <pre><code>radu@ThinkPad-L380$ kubectl -n cixpress get pods\nNAME                                  READY   STATUS    RESTARTS   AGE\ntestapp-59c797ffbd-vhwn9              1/1     Running   0          70d\n</code></pre></p> <p>Note</p> <p>Depending on the specific release name you used with helm install the name of the pods might have different names.</p> <p>Check the ingress for the frontend and for the backend: <pre><code>03:32 PM ~\nradu@ThinkPad-L380$ kubectl -n cixpress get ing\nNAME            CLASS   HOSTS                   ADDRESS         PORTS     AGE\ntestapp-demo    nginx   testapp.virtomat.dev    85.204.43.218   80, 443   235d\n</code></pre></p>"},{"location":"ci-test-repo/configuration/","title":"Configuration for a simple app","text":""},{"location":"ci-test-repo/configuration/#project-template","title":"Project template","text":"<p>Info</p> <p>I'm using a simple python app for test in this exercise. You can clone the source code from <code>git@github.com:virtomat/ci-test-repo.git</code></p> <p>Regardless of the language (python, java, Angular, etc) the project structure is the same. It looks like this:</p> <pre><code>radu@ThinkPad-L380$ tree -L 2\n.\n\u251c\u2500\u2500 docs\n\u2502    \u251c\u2500\u2500 build_and_deploy.md\n\u2502    \u2514\u2500\u2500 configuration.md\n\u251c\u2500\u2500 helm\n\u2502    \u251c\u2500\u2500 Chart.yaml\n\u2502    \u251c\u2500\u2500 templates\n\u2502    \u2502    \u251c\u2500\u2500 deployment.yaml\n\u2502    \u2502    \u251c\u2500\u2500 ingress.yaml\n\u2502    \u2502    \u2514\u2500\u2500 service.yaml\n\u2502    \u2514\u2500\u2500 values.yaml\n\u251c\u2500\u2500 app.py\n\u251c\u2500\u2500 logger.py\n\u251c\u2500\u2500 Dockerfile\n\u251c\u2500\u2500 requirements.txt\n\u2514\u2500\u2500 README.md\n</code></pre> <ul> <li><code>docs</code> - This is a directory dedicated for documentation related to this app. It is usually automatically build to a static html site with mkdocs.</li> <li><code>helm</code> - This is the Helm Chart definition. Notice the templates directory. A basic setup will require a <code>deployment</code> (the actual container in a pod), a service (expose the pod on the internal network) and an ingress (basic layer 7 routing rules, to expose the app outside the cluster)</li> <li><code>app.py</code> - Test app code (Flask app)</li> <li><code>logger.py</code>- Some logging setup I find useful.</li> <li><code>Dockerfile</code> - Dockerfile to build the container</li> <li><code>README.md</code> - Optional Readme file (deprecated in favor of docs directory).</li> </ul>"},{"location":"ci-test-repo/configuration/#pipeline-templates","title":"Pipeline templates","text":"<p>A pipeline template is pretty basic at this point (expected to be extended in functionality), and only specifies the steps in the pipeline.  The definition is supported as <code>json</code> content and we use REST API to push and pull the template definitions.</p> <p>For the <code>Default</code> template the json looks like this: <pre><code>{\n  \"Default\": {\n    \"pvc\": {\n      \"required\": true,\n      \"size\": \"1Gi\"\n    },\n    \"steps\": [\n      {\n        \"icon\": \"feather:github\",\n        \"name\": \"git-clone\"\n      },\n      {\n        \"icon\": \"docker_decolorize\",\n        \"name\": \"kaniko-build\"\n      },\n      {\n        \"icon\": \"helm\",\n        \"name\": \"helm-update\"\n      },\n      {\n        \"icon\": \"heroicons_mini:arrow-path-rounded-square\",\n        \"name\": \"cleanup\"\n      }\n    ]\n  }\n}\n</code></pre></p> <ul> <li><code>pvc</code> - This is the spec for a volume where the workspace will be. All steps in the pipeline will share this workspace.</li> <li><code>steps</code> - This are the steps in the pipeline. The execution will follow the order specified here. The naming is also important, as each step name becomed the prefix in finding the right manifest for the job.</li> </ul> Create/Update a template definition: (expand to see) <pre><code>curl --location 'http://ci.virtomat.dev/conductor/templates' \\\n--header 'Content-Type: application/json' \\\n--data '{\n    \"Default\": {\n        \"pvc\": {\n            \"required\": true,\n            \"size\": \"1Gi\"\n        },\n        \"steps\": [\n            {\n                \"name\": \"git-clone\",\n                \"icon\": \"feather:github\"\n            },\n            {\n                \"name\": \"kaniko-build\",\n                \"icon\": \"docker_decolorize\"\n            },\n            {\n                \"name\": \"helm-update\",\n                \"icon\": \"helm\"\n            },\n            {\n                \"name\": \"cleanup\",\n                \"icon\": \"heroicons_mini:arrow-path-rounded-square\"\n            }\n        ]\n    }\n}'\n</code></pre> Retrieve a template definition (expand to see) <pre><code>curl --location 'https://ci.virtomat.dev/conductor/templates'\n{\n    \"Default\": {\n        \"pvc\": {\n            \"required\": true,\n            \"size\": \"1Gi\"\n        },\n        \"steps\": [\n            {\n                \"icon\": \"feather:github\",\n                \"name\": \"git-clone\"\n            },\n            {\n                \"icon\": \"docker_decolorize\",\n                \"name\": \"kaniko-build\"\n            },\n            {\n                \"icon\": \"helm\",\n                \"name\": \"helm-update\"\n            },\n            {\n                \"icon\": \"heroicons_mini:arrow-path-rounded-square\",\n                \"name\": \"cleanup\"\n            }\n        ]\n    }\n}\n</code></pre> <p>Note</p> <p>You can have as many templates as you want. Each template defines a new kind of pipeline.</p>"},{"location":"ci-test-repo/configuration/#pipeline-configs","title":"Pipeline configs","text":"<p>For the test app the json looks like this: <pre><code>{\n  \"templateName\": \"Default\",\n  \"steps\": {\n    \"git-clone\": {\n      \"git_repo_url\": \"gitlab.com/virtomat/ci-test-repo.git\",\n      \"git_branch\": \"main\",\n      \"logging\": \"DEBUG\",\n      \"webhook_secret\": \"4F2XqXXk\",\n      \"ttlSecondsAfterFinished\": 120\n    },\n    \"kaniko-build\": {\n      \"logging\": \"DEBUG\",\n      \"ttlSecondsAfterFinished\": 120\n    },\n    \"helm-update\": {\n      \"logging\": \"DEBUG\",\n      \"ttlSecondsAfterFinished\": 120\n    },\n    \"cleanup\": {\n      \"logging\": \"DEBUG\",\n      \"docs_repo_url\": \"https://github.com/virtomat/voyager-docs.git\",\n      \"ttlSecondsAfterFinished\": 120\n    }\n  }\n}\n</code></pre></p> <ul> <li><code>templateName</code> - references the template name (it has to exist)</li> <li><code>steps</code> - The steps in the pipeline must follow the naming in the template.<ul> <li><code>git-clone</code> - This step will clone the git repo in the workspace directory.</li> <li><code>kaniko-build</code> - This is the build steps (builds the container and pushes it to the registry)</li> <li><code>helm-update</code> - This is the helm update step (version increment) and published the Chart to registry</li> <li><code>cleanup</code> - Some cleanup tasks.</li> </ul> </li> </ul> <p>Todo</p> <p>We should move <code>ttlSecondsAfterFinished</code> to template, this is a common config, usually applicable for all pipelines of the same kind</p> Curl call example   (exand to see) <pre><code>curl --location 'http://ci.virtomat.dev/conductor/configure' \\\n--header 'Content-Type: application/json' \\\n--data '[\n    {\n        \"templateName\": \"Default\",\n        \"steps\": {\n            \"git-clone\": {\n                \"git_repo_url\": \"gitlab.com/virtomat/ci-test-repo.git\",\n                \"git_branch\": \"main\",\n                \"logging\": \"DEBUG\",\n                \"webhook_secret\": \"4F2XqXXk\",\n                \"ttlSecondsAfterFinished\": 120\n            },\n            \"kaniko-build\": {\n                \"logging\": \"DEBUG\",\n                \"ttlSecondsAfterFinished\": 120\n            },\n            \"helm-update\": {\n                \"logging\": \"DEBUG\",\n                \"ttlSecondsAfterFinished\": 120\n            },\n            \"cleanup\": {\n                \"logging\": \"DEBUG\",\n                \"docs_repo_url\": \"https://github.com/virtomat/voyager-docs.git\",\n                \"ttlSecondsAfterFinished\": 120\n            }\n        }\n    }\n]'\n</code></pre>"},{"location":"ci-test-repo/configuration/#simulating-github-trigger","title":"Simulating github trigger","text":"<p>You can use a <code>curl</code> call to trigger a pipeline (just replace the repo url with your own repo url):</p> GitHub trigger   (expand to see) <pre><code>curl --location 'https://ci.virtomat.dev/conductor/githubTrigger' \\\n--header 'Content-Type: application/json' \\\n--data '{\n    \"ref\": \"refs/heads/main\",  \n    \"repository\": {\n        \"clone_url\": \"https://github.com/virtomat/ci-test-repo.git\"  \n    }\n}'\n</code></pre> GitLab trigger   (expand to see) <pre><code>curl --location 'https://ci.virtomat.dev/conductor/gitlabTrigger' \\\n--header 'Content-Type: application/json' \\\n--data '{\n    \"ref\": \"refs/heads/main\",\n    \"repository\": {\n        \"git_http_url\": \"https://gitlab.com/virtomat/ci-test-repo.git\"\n    }\n}'\n</code></pre>"}]}